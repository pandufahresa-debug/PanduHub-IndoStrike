-- LOAD
if not game:IsLoaded() then
    game.Loaded:Wait()
end

local Players = game:GetService("Players")
local player = Players.LocalPlayer
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local GuiService = game:GetService("GuiService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")
local MarketplaceService = game:GetService("MarketplaceService")
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local Camera = workspace.CurrentCamera
local TeleportService = game:GetService("TeleportService")
local AttackRange = 50

-- ESP COLORS
local SurvivorESPColor = Color3.fromRGB(57,255,20)
local KillerESPColor = Color3.fromRGB(255,40,40)
local ZombieESPColor = Color3.fromRGB(255,140,0)

local GeneratorESPColor = Color3.fromRGB(150,0,200)
local HookESPColor = Color3.fromRGB(132,255,169)
local GateESPColor = Color3.fromRGB(255,255,255)

local PalletESPColor = Color3.fromRGB(74,255,181)
local WindowESPColor = Color3.fromRGB(74,255,181)

local AutoSkillCheckEnabled = false
local GodMode = false
local AutoWiggleEnabled = false
local AutoCarryEnabled = false
local AutoAttackEnabled = false
local BypassGateEnabled = false
local ForceJumpEnabled = false
local ForceJumpHeartbeat
local ForceJumpInput
local ForceJumpCharacter
local FleeKillerEnabled = false
local FleeDistance = 40
local FleeCooldown = 0

local currentTarget = nil
local busy = false
local lastESPScan = 0

-- Variabel untuk Auto Skill Check
local HeartbeatConnection = nil
local VisibilityConnection = nil

-- Variabel untuk Speed Boost
local SpeedEnabled = false
local SpeedAmount = 0.02
local SpeedInputEnabled = false
local SpeedInputValue = 0.1
local SpeedInputConnection = nil

-- Variabel untuk No Clip
local NoclipEnabled = false
local NoclipConnection = nil

-- Variabel untuk Performance
local FullBright = false
local NoFog = false
local NoShadow = false
local LowGraphics = false
local FPSBoost = false
local TimeOfDayValue = 14
local savedGraphics = {}
local savedEffects = {}

-- Variabel untuk Emote
local EmoteEnabled = false
local SelectedAnim = nil
local SelectedSound = nil
local currentTrack = nil
local currentSound = nil

-- Variabel untuk AFK
local AFK_Thread = nil
local PreviousTeam = LocalPlayer.Team and LocalPlayer.Team.Name or "Unknown"

-- Variabel ConfigData untuk skill check
local ConfigData = {
    Surv_SkillFrequency = 10,
    Surv_SkillSpeed = 1
}

-- ==================== OPTIMIZED CACHE SYSTEM ====================
local cache = {
    Generator = {}, Hook = {}, Gate = {}, Window = {}, Pallet = {},
    Zombie = {}, SCPs = {}
}

local function IsZombie(model)
    if not model or not model:IsA("Model") then return false end
    local name = model.Name:lower()
    return name == "scp" or name == "scp2" or name == "scp3" or name == "scp4" 
        or name == "scp5" or name == "scp6" or name == "scp7" or name == "scp8"
end

local function AddToCache(obj)
    local category = nil
    if obj.Name == "Generator" then category = "Generator"
    elseif obj.Name == "Hook" then category = "Hook"
    elseif obj.Name == "Gate" then category = "Gate"
    elseif obj.Name == "Window" then category = "Window"
    elseif obj.Name == "Pallet" or obj.Name == "Palletwrong" then category = "Pallet"
    elseif IsZombie(obj) then category = "Zombie" end

    if category then
        local targetPart = (obj:IsA("Model") and (obj.PrimaryPart or obj:FindFirstChildWhichIsA("BasePart"))) 
                        or (obj:IsA("BasePart") and obj)
        if targetPart then
            table.insert(cache[category], { obj = obj, part = targetPart })
        end
    end
end

local function RemoveFromCache(removingObj)
    for cat, list in pairs(cache) do
        for i = #list, 1, -1 do
            local data = list[i]

            if typeof(data) == "table" and data.obj == removingObj then
                table.remove(list, i)
            end
        end
    end
end

for _, v in pairs(workspace:GetDescendants()) do
    AddToCache(v)
end

workspace.DescendantAdded:Connect(AddToCache)
workspace.DescendantRemoving:Connect(RemoveFromCache)

local function ScanMap()
    table.clear(cache.SCPs)
    local workspaceDescendants = workspace:GetDescendants()
    for i, v in ipairs(workspaceDescendants) do
        if i % 200 == 0 then task.wait() end
        if v:IsA("Model") and string.match(string.lower(v.Name), "^scp") then
            if not table.find(cache.SCPs, v) then
                table.insert(cache.SCPs, v)
            end
        end
    end
end

ScanMap()

workspace.DescendantAdded:Connect(function(v)
    if v:IsA("Model") then
        local name = string.lower(v.Name)
        if string.match(name, "^scp") then
            if not table.find(cache.SCPs, v) then
                table.insert(cache.SCPs, v)
            end
        end
    end
end)

local isAimbotHolding = false
local lockedTarget = nil

-- [ VARIABEL AUTO PARRY & SILENT AIM ]
local Config = {
    Surv_AutoParry = false,
    Surv_ParryAggressive = false,
    Surv_ParryCircle = true,
    Surv_ParryRadius = 15,
    Surv_ParryFace = 0.7,
    Ignored_Skills_List = {},
    LockAim = false,
    Killer_3rdPerson = false,
    AUTO_AFKEscape = false,
    Webhook_Enabled = false,
    Webhook_URL = "",
    Surv_Aimbot_Enabled = false,
    Surv_Aimbot_ShowFOV = true,
    Surv_Aimbot_Radius = 150,
    Surv_Aimbot_MaxDist = 300,
    Surv_Aimbot_Smoothness = 0.5,
    Surv_Aimbot_Predict = 0.01,
    Killer_Aimbot_Enabled = false,
    Killer_Aimbot_MaxDist = 12,
    Killer_Aimbot_Smoothness = 0.5,
    Misc_FakeName = false,
    Surv_AutoCrouch = false,
    Surv_InstanSkillCheck = false
}

local AimConfig = {
    Aim_Silent = false,
    LockAim = false,
    Pistol_Target = "Survivor",
    Pistol_FOVMode = false,
    Pistol_ShowFOV = false,
    Pistol_FOV = 150,
    Aim_SilentVeil = false,
    Veil_ShowFOV = true,
    Veil_FOV = 150,
    SPEAR_Speed = 165,
    SPEAR_Gravity = workspace.Gravity * 0.5,
    SPEAR_MaxDist = 200,
    AIM_Auto = false,
    AIM_TargetPart = "Torso"
}

local State = { 
    ParryCooldown = false,
    ParryCooldownTime = 60,
    AutoParryAdornment = nil,
    ThirdPersonConn = nil,
    SpooferConns = {}
}
local CachedInstnScript = nil

local VALID_PARRY_IDS = {
    ["122812055447896"] = "Veil lunge",
    ["133963973694098"] = "Mayers Basic",
    ["117042998468241"] = "Mayers lunge",
    ["135002183282873"] = "cure lunge",
    ["121216847022485"] = "cure Basic",
    ["132817836308238"] = "Jeff Basic",
    ["129784271201071"] = "Jeff lunge",
    ["82666958311998"] = "Jeff Frenzy",
    ["78432063483146"] = "Abyssal Basic",
    ["118907603246885"] = "Abyssal lunge",
    ["139369275981139"] = "Jason Basic",
    ["110355011987939"] = "Jason lunge",
    ["111920872708571"] = "Masked Basic",
    ["105374834496520"] = "Masked lunge",
    ["138720291317243"] = "Masked Tony",
    ["106871536134254"] = "Masked Alex",
    ["130593238885843"] = "Masked Cobra",
    ["115244153053858"] = "Masked Cobra lunge",
    ["74968262036854"] = "Hidden Basic",
    ["113255068724446"] = "Hidden lunge",
    ["98163597193511"] = "Hidden S1",
    ["80411309607666"] = "Abyssal S1"
}

local Attached = {}
local armParts = {
    ["Left Arm"] = true, ["Right Arm"] = true,
    ["LeftHand"] = true, ["RightHand"] = true,
    ["LeftLowerArm"] = true, ["RightLowerArm"] = true,
    ["LeftUpperArm"] = true, ["RightUpperArm"] = true
}

local function enableSpoofer() 
    if not Config.Misc_FakeName then return end
    local PlayerGui = LocalPlayer:FindFirstChild("PlayerGui")
    if not PlayerGui then return end
    
    local function applySpooferToObj(obj)
        if obj:IsA("TextLabel") or obj:IsA("TextButton") or obj:IsA("TextBox") then
            local function updateText()
                if not Config.Misc_FakeName then return end
                local currentText = obj.Text
                local changed = false

                if not obj:GetAttribute("OriginalText") then
                    obj:SetAttribute("OriginalText", currentText)
                end

                for _, player in ipairs(Players:GetPlayers()) do
                    if string.find(currentText, player.Name) or string.find(currentText, player.DisplayName) then
                        currentText = string.gsub(currentText, player.Name, "PANDU HUB")
                        currentText = string.gsub(currentText, player.DisplayName, "PANDU HUB")
                        changed = true
                    end
                end
                
                if changed and obj.Text ~= currentText then
                    obj.Text = currentText
                end
            end

            updateText()
            local conn = obj:GetPropertyChangedSignal("Text"):Connect(updateText)
            table.insert(State.SpooferConns, conn)
            
        elseif obj:IsA("ImageLabel") or obj:IsA("ImageButton") then
            local function updateImage()
                if not Config.Misc_FakeName then return end
                local currentImg = obj.Image
                local changed = false

                for _, player in ipairs(Players:GetPlayers()) do
                    if string.find(currentImg, tostring(player.UserId)) then
                        if not obj:GetAttribute("OriginalImage") then
                            obj:SetAttribute("OriginalImage", currentImg)
                        end
                        changed = true
                        break 
                    end
                end

                if changed and obj.Image ~= "rbxassetid://109651670989001" then
                    obj.Image = "rbxassetid://109651670989001"
                end
            end

            updateImage() 
            local conn = obj:GetPropertyChangedSignal("Image"):Connect(updateImage)
            table.insert(State.SpooferConns, conn)
        end
    end
    
    for _, obj in ipairs(PlayerGui:GetDescendants()) do applySpooferToObj(obj) end
    local conn = PlayerGui.DescendantAdded:Connect(function(obj) task.defer(function() applySpooferToObj(obj) end) end)
    table.insert(State.SpooferConns, conn) 
end

local function disableSpoofer() 
    for _, conn in ipairs(State.SpooferConns) do 
        if conn.Connected then conn:Disconnect() end 
    end
    table.clear(State.SpooferConns) 
    
    local PlayerGui = LocalPlayer:FindFirstChild("PlayerGui")
    if PlayerGui then
        for _, obj in ipairs(PlayerGui:GetDescendants()) do
            if obj:IsA("TextLabel") or obj:IsA("TextButton") or obj:IsA("TextBox") then
                local oriText = obj:GetAttribute("OriginalText")
                if oriText then
                    obj.Text = oriText
                    obj:SetAttribute("OriginalText", nil)
                end
            elseif obj:IsA("ImageLabel") or obj:IsA("ImageButton") then
                local oriImage = obj:GetAttribute("OriginalImage")
                if oriImage then
                    obj.Image = oriImage
                    obj:SetAttribute("OriginalImage", nil)
                end
            end
        end
    end
end
local function TriggerCrouch()
    pcall(function()
        local b = LocalPlayer:FindFirstChild("PlayerGui")
        for segment in string.gmatch("Survivor-mob.Controls.crouch.icon", "[^%.]+") do 
            if b then b = b:FindFirstChild(segment) end
        end

        if b and b:IsA("GuiObject") and b.Visible and b.Parent and b.Parent:IsA("GuiButton") then
            local btn = b.Parent
            if type(firesignal) == "function" then
                pcall(function() firesignal(btn.MouseButton1Click) end)
            else
                local center = btn.AbsolutePosition + (btn.AbsoluteSize / 2)
                local inset = GuiService:GetGuiInset()
                local x = center.X + inset.X
                local y = center.Y + inset.Y
                
                VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
                task.wait(0.02)
                VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
            end
        else
            VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.LeftControl, false, game)
            task.wait(0.02)
            VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.LeftControl, false, game)
        end
    end)
end
local function isWeapon(obj)
    if obj:IsA("Tool") then return true end
    if obj.Parent and armParts[obj.Parent.Name] then return true end
    local name = obj.Name:lower()
    if name:match("weapon") or name:match("gun") or name:match("blade") then return true end
    return false
end
local function GetRole()
    if not LocalPlayer.Team then return "Unknown" end
    local name = LocalPlayer.Team.Name
    if name == "Killer" then return "Killer"
    elseif name == "Survivors" then return "Survivor"
    else return "Spectator" end
end
local function IsSurvivor(p) return p and p.Team and p.Team.Name == "Survivors" end
local function IsKiller(p) return p and p.Team and p.Team.Name == "Killer" end
local function IsDowned(char)
    if not char then return false end
    return char:GetAttribute("Knocked") == true or char:GetAttribute("IsHooked") == true
end

local function faceTarget(targetPart, smoothness)
    if not targetPart then return end
    smoothness = smoothness or 0.1
    local targetCFrame = CFrame.lookAt(Camera.CFrame.Position, targetPart.Position)
    Camera.CFrame = Camera.CFrame:Lerp(targetCFrame, smoothness)
end
local function GetDistance(pos) 
    local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not root then return math.huge end
    return (pos - root.Position).Magnitude 
end
-- Helper functions untuk Auto Carry
local function GetDownedChar()
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= player and plr.Character and IsSurvivor(plr) then
            local hum = plr.Character:FindFirstChildOfClass("Humanoid")
            if hum and hum.Health <= 0 then
                return plr.Character
            end
        end
    end
    return nil
end

local function GetNearestHook()
    local myChar = player.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if not myHRP then return nil end
    
    local closest = nil
    local shortest = math.huge
    
    for _, data in pairs(cache.Hook) do
        if data and data.part then
            local dist = (data.part.Position - myHRP.Position).Magnitude
            if dist < shortest then
                shortest = dist
                closest = data.part
            end
        end
    end
    return closest
end

local CarryEvent = ReplicatedStorage:FindFirstChild("Remotes") and 
                   ReplicatedStorage.Remotes:FindFirstChild("Carry") and 
                   ReplicatedStorage.Remotes.Carry:FindFirstChild("CarryEvent")
local HookEvent = ReplicatedStorage:FindFirstChild("Remotes") and 
                  ReplicatedStorage.Remotes:FindFirstChild("Carry") and 
                  ReplicatedStorage.Remotes.Carry:FindFirstChild("HookEvent")

local function GetClosestEnemy()
    local closest = nil
    local shortest = AttackRange
    local myChar = player.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if not myHRP then return nil end
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= player and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
            local hrp = plr.Character.HumanoidRootPart
            local team = plr.Team and plr.Team.Name:lower() or ""
            if team ~= (player.Team and player.Team.Name:lower()) then
                local dist = (hrp.Position - myHRP.Position).Magnitude
                if dist < shortest then 
                    shortest = dist 
                    closest = plr 
                end
            end
        end
    end
    return closest, shortest
end

local function GetNearestAliveSurvivor()
    local closest = nil
    local shortest = math.huge
    local myHRP = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
    if not myHRP then return nil end
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= player and plr.Character then
            local hum = plr.Character:FindFirstChildOfClass("Humanoid")
            local hrp = plr.Character:FindFirstChild("HumanoidRootPart")
            if hum and hrp and hum.Health > 0 and hum.Health > 30 then
                local d = (hrp.Position - myHRP.Position).Magnitude
                if d < shortest then 
                    shortest = d 
                    closest = plr.Character 
                end
            end
        end
    end
    return closest
end

if CoreGui:FindFirstChild("FOVCircleGui_Standalone") then 
    CoreGui:FindFirstChild("FOVCircleGui_Standalone"):Destroy() 
end

local FOVGui = Instance.new("ScreenGui")
FOVGui.Name = "FOVCircleGui_Standalone"
FOVGui.Parent = CoreGui
FOVGui.ResetOnSpawn = false
FOVGui.IgnoreGuiInset = true

local FOVFrame = Instance.new("Frame")
FOVFrame.BackgroundTransparency = 1
FOVFrame.AnchorPoint = Vector2.new(0.5, 0.5)
FOVFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
FOVFrame.Visible = false
FOVFrame.Parent = FOVGui

local FOVCorner = Instance.new("UICorner")
FOVCorner.CornerRadius = UDim.new(1, 0)
FOVCorner.Parent = FOVFrame

local FOVStroke = Instance.new("UIStroke")
FOVStroke.Color = Color3.fromRGB(0, 255, 100)
FOVStroke.Thickness = 1.5
FOVStroke.Parent = FOVFrame

local function getBestAimbotTarget()
    local bestTarget = nil
    local myRole = GetRole()
    
    local shortestFovDist = Config.Surv_Aimbot_Radius
    local shortest3DDist = math.huge
    local centerScreen = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
            local isValidTarget = false
            
            -- Filter peran
            if myRole == "Survivor" and Config.Surv_Aimbot_Enabled and IsKiller(p) then
                isValidTarget = true
            elseif myRole == "Killer" and Config.Killer_Aimbot_Enabled and not IsKiller(p) then
                local hum = p.Character:FindFirstChild("Humanoid")
                if hum and hum.Health > 0 and not IsDowned(p.Character) then
                    isValidTarget = true
                end
            end
            
            if isValidTarget then 
                local hrp = p.Character.HumanoidRootPart
                local dist3D = GetDistance(hrp.Position)
                
                if myRole == "Survivor" and dist3D <= Config.Surv_Aimbot_MaxDist then
                    local screenPos, onScreen = Camera:WorldToViewportPoint(hrp.Position)
                    if onScreen then 
                        local targetPos2D = Vector2.new(screenPos.X, screenPos.Y)
                        local dist2D = (targetPos2D - centerScreen).Magnitude
                        if dist2D < shortestFovDist then 
                            shortestFovDist = dist2D
                            bestTarget = hrp 
                        end 
                    end
                    
                elseif myRole == "Killer" and dist3D <= Config.Killer_Aimbot_MaxDist then
                    if dist3D < shortest3DDist then 
                        shortest3DDist = dist3D
                        bestTarget = hrp
                    end
                end
            end
        end
    end
    return bestTarget
end

local function GetKillerUI()
    local playerGui = LocalPlayer:FindFirstChild("PlayerGui")
    if not playerGui then return nil end
    for _, gui in pairs(playerGui:GetChildren()) do
        if string.sub(gui.Name, -4) == "-mob" and gui.Name ~= "Survivor-mob" then
            return gui
        end
    end
    return nil
end

local currentTouchInput = nil

UserInputService.InputBegan:Connect(function(input, gameProcessedEvent)
    local isTouch = (input.UserInputType == Enum.UserInputType.Touch)
    local myRole = GetRole()
    
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        if gameProcessedEvent then return end 
        
        if myRole == "Killer" and Config.Killer_Aimbot_Enabled then
            lockedAimbotTarget = getBestAimbotTarget()
            if lockedAimbotTarget then 
                isAimbotHolding = true 
            end
        end
    end
    
    if isTouch then
        if myRole == "Killer" and Config.Killer_Aimbot_Enabled then
            local killerUI = GetKillerUI()
            local attackBtn = killerUI and killerUI:FindFirstChild("Controls") and killerUI.Controls:FindFirstChild("attack")
            
            if attackBtn and attackBtn.Visible then
                local pos = input.Position
                local absPos = attackBtn.AbsolutePosition
                local absSize = attackBtn.AbsoluteSize
                
                if pos.X >= absPos.X and pos.X <= (absPos.X + absSize.X) and pos.Y >= absPos.Y and pos.Y <= (absPos.Y + absSize.Y) then
                    lockedAimbotTarget = getBestAimbotTarget()
                    if lockedAimbotTarget then 
                        isAimbotHolding = true 
                        currentTouchInput = input
                    end
                end
            end
        end
    end
end)

UserInputService.InputEnded:Connect(function(input, gameProcessedEvent)
    local isTouchEnd = (input.UserInputType == Enum.UserInputType.Touch)
    
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        isAimbotHolding = false
        lockedAimbotTarget = nil
    end
    
    if isTouchEnd and input == currentTouchInput then
        isAimbotHolding = false
        lockedAimbotTarget = nil
        currentTouchInput = nil
    end
end)

RunService.RenderStepped:Connect(function()
    local myChar = LocalPlayer.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
    local myRole = GetRole()
    
    if myChar and myHRP then
        if myRole == "Survivor" and Config.Surv_Aimbot_Enabled then
            local target = getBestAimbotTarget()
            if target then
                local targetPos = target.Position
                local dist = GetDistance(targetPos)
                
                if dist < 100 then 
                    local distanceDiff = 100 - dist
                    local rightOffset = distanceDiff * Config.Surv_Aimbot_Predict
                    targetPos = targetPos + (Camera.CFrame.RightVector * rightOffset) 
                end
                
                local goalCameraCFrame = CFrame.new(Camera.CFrame.Position, targetPos)
                Camera.CFrame = Camera.CFrame:Lerp(goalCameraCFrame, Config.Surv_Aimbot_Smoothness)
                
                local goalHrpCFrame = CFrame.new(myHRP.Position, Vector3.new(targetPos.X, myHRP.Position.Y, targetPos.Z))
                myHRP.CFrame = myHRP.CFrame:Lerp(goalHrpCFrame, Config.Surv_Aimbot_Smoothness)
            end
            
        elseif myRole == "Killer" and Config.Killer_Aimbot_Enabled then
            if isAimbotHolding and lockedAimbotTarget then
                local targetChar = lockedAimbotTarget.Parent
                local targetHum = targetChar and targetChar:FindFirstChild("Humanoid")
                
                if not targetHum or targetHum.Health <= 0 or IsDowned(targetChar) then
                    lockedAimbotTarget = getBestAimbotTarget()
                end

                if lockedAimbotTarget then
                    local targetPos = lockedAimbotTarget.Position
                    if GetDistance(targetPos) > Config.Killer_Aimbot_MaxDist then
                        lockedAimbotTarget = getBestAimbotTarget()
                    else
                        local goalCameraCFrame = CFrame.new(Camera.CFrame.Position, targetPos)
                        Camera.CFrame = Camera.CFrame:Lerp(goalCameraCFrame, Config.Killer_Aimbot_Smoothness)
                        
                        local goalHrpCFrame = CFrame.new(myHRP.Position, Vector3.new(targetPos.X, myHRP.Position.Y, targetPos.Z))
                        myHRP.CFrame = myHRP.CFrame:Lerp(goalHrpCFrame, Config.Killer_Aimbot_Smoothness)
                    end
                end
            else
                lockedAimbotTarget = nil
            end
        end
    end
    
    local showFOV = false
    local centerScreen = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    
    if myRole == "Survivor" and Config.Surv_Aimbot_Enabled and Config.Surv_Aimbot_ShowFOV then
        showFOV = true
        local radius = Config.Surv_Aimbot_Radius
        FOVFrame.Size = UDim2.new(0, radius * 2, 0, radius * 2)
        FOVFrame.Position = UDim2.new(0, centerScreen.X, 0, centerScreen.Y)
        
        if getBestAimbotTarget() ~= nil then
            FOVStroke.Color = Color3.fromRGB(255, 0, 0)
        else
            FOVStroke.Color = Color3.fromRGB(0, 255, 100)
        end
    end
    
    FOVFrame.Visible = showFOV
end)
-- ==================== FUNGSI WEBHOOK ====================
local function censorUsername(name)
    if #name <= 2 then
        return string.rep("*", #name)
    end
    return name:sub(1,2) .. string.rep("*", #name - 2)
end

local function SendWebhook()
    if not Config.Webhook_Enabled or Config.Webhook_URL == "" then return end
    
    task.spawn(function()
        local lp = Players.LocalPlayer
        
        local exp = tostring(lp:GetAttribute("EXP") or "0")
        local level = tostring(lp:GetAttribute("Level") or "0")
        local gears = tostring(lp:GetAttribute("Gears") or "0")
        local screws = tostring(lp:GetAttribute("Screws") or lp:GetAttribute("Screw") or "0")
        local username = censorUsername(lp.Name)
        
        local data = {
            ["content"] = "",
            ["embeds"] = {{
                ["title"] = "📊 Statistik Auto Farm",
                ["description"] = "Auto Farm!",
                ["type"] = "rich",
                ["color"] = 65280,
                ["fields"] = {
                    {["name"] = "👤 Username", ["value"] = username, ["inline"] = false},
                    {["name"] = "📊 Level", ["value"] = level, ["inline"] = true},
                    {["name"] = "⭐ EXP", ["value"] = exp, ["inline"] = true},
                    {["name"] = "⚙️ Gears", ["value"] = gears, ["inline"] = true},
                    {["name"] = "🔩 Screws", ["value"] = screws, ["inline"] = true}
                },
                ["footer"] = {["text"] = "Pandu Hub Script"}
            }}
        }
        
        pcall(function()
            local req = (syn and syn.request) or (http and http.request) or http_request or request or fluxus.request
            if req then
                req({
                    Url = Config.Webhook_URL,
                    Method = "POST",
                    Headers = { ["Content-Type"] = "application/json" },
                    Body = HttpService:JSONEncode(data)
                })
            end
        end)
    end)
end

-- ==================== FUNGSI AFK ESCAPE ====================
local function GetCharacterRoot()
    local char = Players.LocalPlayer.Character
    if char then
        return char:FindFirstChild("HumanoidRootPart")
    end
    return nil
end

local function TeleportToEscape()
    local root = GetCharacterRoot()
    local map = workspace:FindFirstChild("Map")
    if not root or not map then return false end
    
    local exitPos = nil
    pcall(function()
        if map:FindFirstChild("RooftopHitbox") or map:FindFirstChild("Rooftop") then 
            exitPos = Vector3.new(3098.16, 454.04, -4918.74)
            return 
        end
        if map:FindFirstChild("HooksMeat") then 
            exitPos = Vector3.new(1546.12, 152.21, -796.72)
            return 
        end
        if map:FindFirstChild("churchbell") then 
            exitPos = Vector3.new(760.98, -20.14, -78.48)
            return 
        end
        
        local finish = map:FindFirstChild("Finishline") or map:FindFirstChild("FinishLine") or map:FindFirstChild("Fininshline")
        if finish then 
            exitPos = finish:IsA("BasePart") and finish.Position or (finish:FindFirstChildWhichIsA("BasePart") and finish:FindFirstChildWhichIsA("BasePart").Position) 
            return 
        end
        
        for _, obj in ipairs(map:GetDescendants()) do 
            if obj.Name:lower():find("finish") then 
                if obj:IsA("BasePart") then 
                    exitPos = obj.Position
                    break 
                elseif obj:IsA("Model") and obj:FindFirstChildWhichIsA("BasePart") then 
                    exitPos = obj:FindFirstChildWhichIsA("BasePart").Position
                    break 
                end 
            end 
        end
        
        if not exitPos then 
            for _, obj in ipairs(map:GetDescendants()) do 
                if obj:IsA("MeshPart") and obj.Material == Enum.Material.Limestone then 
                    exitPos = Vector3.new(-947.90, 152.12, -7579.52)
                    break 
                end
                if obj:IsA("MeshPart") and obj.Material == Enum.Material.Leather then 
                    exitPos = Vector3.new(1546.12, 152.21, -796.72)
                    break 
                end 
            end 
        end
    end)
    
    if exitPos then 
        root.CFrame = CFrame.new(exitPos + Vector3.new(0, 3, 0)) 
        return true
    end
    return false
end

local function RandomServerHop()
    local success, result = pcall(function() 
        return game:HttpGet("https://games.roblox.com/v1/games/" .. tostring(game.PlaceId) .. "/servers/Public?sortOrder=Asc&limit=100") 
    end)
    
    if success and result then
        local data = HttpService:JSONDecode(result)
        local validServers = {}
        
        if data and data.data then
            for _, server in ipairs(data.data) do
                if type(server) == "table" and server.playing and server.maxPlayers then
                    if server.playing < (server.maxPlayers - 1) and server.id ~= game.JobId then
                        table.insert(validServers, server.id)
                    end
                end
            end
        end
        
        if #validServers > 0 then
            local randomServer = validServers[math.random(1, #validServers)]
            TeleportService:TeleportToPlaceInstance(game.PlaceId, randomServer, LocalPlayer)
        end
    end
end
local function HandleAFKLogic(currentTeam)
    if not Config.AUTO_AFKEscape then return end
    if AFK_Thread then task.cancel(AFK_Thread) end

    if currentTeam == "Survivors" then
        AFK_Thread = task.spawn(function()
            if Library then Library:Notify("AFK Mode ON!.", 3) end
            task.wait(15)
            if Config.AUTO_AFKEscape and LocalPlayer.Team and LocalPlayer.Team.Name == "Survivors" then
                if TeleportToEscape() then
                    if Library then Library:Notify("Berhasil teleport ke exit!", 3) end
                    SendWebhook()
                else
                    if Library then Library:Notify("Gagal menemukan exit!", 3) end
                end
            end
            task.wait(10)
            if Config.AUTO_AFKEscape and LocalPlayer.Team and LocalPlayer.Team.Name == "Survivors" then
                if Library then Library:Notify("Match (Stuck), mencari server baru...", 3) end
                RandomServerHop()
            end
        end)

    elseif currentTeam == "Spectator" then
        AFK_Thread = task.spawn(function()
            if PreviousTeam == "Survivors" then
                if Library then Library:Notify("Match selesai, langsung mencari server baru...", 3) end
                task.wait(5)
                RandomServerHop()
            else
                local isMatchOngoing = false
                for _, p in ipairs(Players:GetPlayers()) do
                    if p.Team and (p.Team.Name == "Survivors" or p.Team.Name == "Killer") then
                        isMatchOngoing = true
                        break
                    end
                end

                if isMatchOngoing then
                    if Library then Library:Notify("Match sedang berlangsung, mencari server baru...", 3) end
                    task.wait(2)
                    RandomServerHop()
                else
                    if Library then Library:Notify("Menunggu match mulai...", 3) end
                end
            end
        end)

    elseif currentTeam == "Killer" then
        AFK_Thread = task.spawn(function()
            if Library then Library:Notify("Terpilih menjadi Killer! Mencari server baru...", 3) end
            task.wait(3)
            RandomServerHop()
        end)
    end
    
    PreviousTeam = currentTeam
end
-- ==================== AUTO SKILL CHECK ====================
local function TriggerMobileButton()
    pcall(function()
        local b = LocalPlayer:FindFirstChild("PlayerGui")
        for segment in string.gmatch("Survivor-mob.Controls.action.check", "[^%.]+") do 
            if b then b = b:FindFirstChild(segment) end
        end

        if b and b:IsA("GuiObject") and b.Visible and b.Parent and b.Parent:IsA("GuiButton") then
            local btn = b.Parent
            
            if type(firesignal) == "function" then
                pcall(function()
                    firesignal(btn.MouseButton1Down)
                    task.wait(0.01)
                    firesignal(btn.MouseButton1Up)
                end)
                
            else
                local center = btn.AbsolutePosition + (btn.AbsoluteSize / 2)
                local inset = GuiService:GetGuiInset()
                local x = center.X + inset.X
                local y = center.Y + inset.Y
                
                VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
                task.wait(0.02)
                VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
            end
        else
            VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Space, false, game)
            task.wait(0.012)
            VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.Space, false, game)
        end
    end)
end

local function InitializeAutoSkillCheck()
    task.spawn(function()
        local PlayerGui = LocalPlayer:FindFirstChild("PlayerGui")
        local prompt = PlayerGui:WaitForChild("SkillCheckPromptGui", 10)
        if not prompt then return end
        
        local check = prompt:WaitForChild("Check", 8)
        if not check then return end
        
        local line = check:WaitForChild("Line", 5)
        local goal = check:WaitForChild("Goal", 5)
        if not (line and goal) then return end
        
        if VisibilityConnection then VisibilityConnection:Disconnect() end
        
        VisibilityConnection = check:GetPropertyChangedSignal("Visible"):Connect(function()
            if not (LocalPlayer.Team and LocalPlayer.Team.Name == "Survivors") or not AutoSkillCheckEnabled then 
                if HeartbeatConnection then HeartbeatConnection:Disconnect() HeartbeatConnection = nil end 
                return 
            end
            
            if check.Visible then
                if HeartbeatConnection then HeartbeatConnection:Disconnect() end
                
                local lastHitTick = 0 
                HeartbeatConnection = RunService.Heartbeat:Connect(function()
                    if not AutoSkillCheckEnabled then
                        HeartbeatConnection:Disconnect() 
                        HeartbeatConnection = nil 
                        return
                    end
                    
                    local lr = line.Rotation % 360
                    local gr = goal.Rotation % 360
                    local ss = (gr + 105) % 360
                    local se = (gr + 115) % 360
                    
                    local inZone = (ss > se and (lr >= ss or lr <= se)) or (lr >= ss and lr <= se)
                    
                    if inZone then
                        if tick() - lastHitTick > 0.2 then
                            TriggerMobileButton()
                            lastHitTick = tick() 
                        end
                    end
                end)
            else
                if HeartbeatConnection then HeartbeatConnection:Disconnect() HeartbeatConnection = nil end
            end
        end)
    end)
end
-- ==================== AUTO PARRY SENSOR ====================
local function tapMobileParryButton()
    local playerGui = LocalPlayer:FindFirstChild("PlayerGui")
    if not playerGui then return end

    local survivorMob = playerGui:FindFirstChild("Survivor-mob")
    local parryBtn = survivorMob
        and survivorMob:FindFirstChild("Controls")
        and survivorMob.Controls:FindFirstChild("Gui-mob")

    if parryBtn and parryBtn.Visible then
        if firesignal then
            pcall(function()
                firesignal(parryBtn.MouseButton1Down)
                task.wait(0.01)
                firesignal(parryBtn.MouseButton1Up)
            end)
        end
    else
        pcall(function()
            if mouse2click then
                mouse2click()
                return
            end
            if mouse2press and mouse2release then
                mouse2press()
                task.wait(0.01)
                mouse2release()
                return
            end
            if MouseButton2Click then
                MouseButton2Click()
                return
            end
            VirtualInputManager:SendMouseButtonEvent(0, 0, 1, true, game, 0)
            task.wait(0.01)
            VirtualInputManager:SendMouseButtonEvent(0, 0, 1, false, game, 0)
        end)
    end
end

local function ExecuteParry()
    if State.ParryCooldown then return end
    pcall(function()
        local parryRemote = game:GetService("ReplicatedStorage"):FindFirstChild("Remotes"):FindFirstChild("Items"):FindFirstChild("Parrying Dagger"):FindFirstChild("parry")
        if parryRemote then
            for i = 1, 10 do parryRemote:FireServer() end
        end
        task.spawn(tapMobileParryButton)
    end)
end
local function ListenToParryResult()
    task.spawn(function()
        local remotes = game:GetService("ReplicatedStorage"):WaitForChild("Remotes", 5)
        local dagger = remotes and remotes:WaitForChild("Items", 5):WaitForChild("Parrying Dagger", 5)
        local parryResultRemote = dagger and dagger:WaitForChild("parryResult", 5)
        
        if parryResultRemote then
            parryResultRemote.OnClientEvent:Connect(function(arg1, arg2)
                local cdDur = tonumber(arg2) or ((arg1 == true) and 90 or 60)
                State.ParryCooldown = true
                if State.ParryCooldownThread then task.cancel(State.ParryCooldownThread) end
                State.ParryCooldownThread = task.delay(cdDur, function()
                    State.ParryCooldown = false
                end)
            end)
        end
    end)
end
ListenToParryResult()

local function AttachParrySensor(kChar)
    if not kChar or Attached[kChar] then return end
    Attached[kChar] = true
    local humanoid = kChar:FindFirstChild("Humanoid")
    if not humanoid then
        humanoid = kChar:WaitForChild("Humanoid", 5)
        if not humanoid then return end
    end
    local animator = humanoid:FindFirstChildOfClass("Animator")
    if not animator then
        animator = humanoid:WaitForChild("Animator", 5)
        if not animator then return end
    end

    humanoid.ChildAdded:Connect(function(child)
        if child:IsA("Animator") then
            Attached[kChar] = nil
            AttachParrySensor(kChar)
        end
    end)

    kChar.AncestryChanged:Connect(function(_, parent)
        if not parent then
            Attached[kChar] = nil
        end
    end)

    animator.AnimationPlayed:Connect(function(track)
        local animId = track.Animation and track.Animation.AnimationId or ""
        local id = animId:match("%d+")
        local attackName = VALID_PARRY_IDS[id]
        if not attackName then return end
        -- [ TAMBAHAN LOGIKA AUTO CROUCH ]
        if id == "80411309607666" and Config.Surv_AutoCrouch then
            local myChar = LocalPlayer.Character
            if IsDowned(myChar) then return end
            local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
            local kHRP = kChar:FindFirstChild("HumanoidRootPart")
            if myHRP and kHRP then
                local dist = (myHRP.Position - kHRP.Position).Magnitude
                if dist <= 30 then
                    TriggerCrouch()
                    task.delay(1.5, function() TriggerCrouch() end)
                end
            end
            return 
        end
        
        if not Config.Surv_AutoParry then return end
        if State.ParryCooldown then return end 
        if Config.Ignored_Skills_List and Config.Ignored_Skills_List[attackName] then return end

        local myChar = LocalPlayer.Character
        if IsDowned(myChar) then return end
        local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
        local kHRP = kChar:FindFirstChild("HumanoidRootPart")
        if not myHRP or not kHRP then return end
        
        local delta = myHRP.Position - kHRP.Position
        local startDistance = delta.Magnitude

        if Config.Surv_ParryAggressive then
            local aggressiveRadius = 12
            local detectionRadius = Config.Surv_ParryRadius + 5
            if startDistance > detectionRadius then return end
            if startDistance <= aggressiveRadius then
                ExecuteParry()
            else
                local tracker
                local startTime = os.clock()
                tracker = RunService.Heartbeat:Connect(function()
                    if os.clock() - startTime >= 1.5 or State.ParryCooldown or not myHRP or not kHRP or IsDowned(myChar) then
                        if tracker then tracker:Disconnect() end
                        return
                    end
                    local currentDist = (myHRP.Position - kHRP.Position).Magnitude
                    if currentDist <= aggressiveRadius then
                        ExecuteParry()
                        if tracker then tracker:Disconnect() end
                    end
                end)
            end
        else
            if startDistance > Config.Surv_ParryRadius then return end
            local myPosFlat = Vector3.new(myHRP.Position.X, 0, myHRP.Position.Z)
            local kPosFlat = Vector3.new(kHRP.Position.X, 0, kHRP.Position.Z)
            local flatDelta = myPosFlat - kPosFlat
            if flatDelta.Magnitude > 0 then
                local flatDirection = flatDelta.Unit
                local kLookFlat = Vector3.new(kHRP.CFrame.LookVector.X, 0, kHRP.CFrame.LookVector.Z).Unit
                local isFacing = kLookFlat:Dot(flatDirection)
                if isFacing < Config.Surv_ParryFace then return end
            end
            ExecuteParry()
        end
    end)
end

local function TryAttach(p)
    if p ~= player and IsKiller(p) and p.Character then 
        AttachParrySensor(p.Character) 
    end
end

local function SetupPlayer(p)
    if p == player then return end
    p.CharacterAdded:Connect(function() TryAttach(p) end)
    p:GetPropertyChangedSignal("Team"):Connect(function() TryAttach(p) end)
    if p.Character then TryAttach(p) end
end

-- ==================== SILENT AIM ====================
local isChargingSpear = false
local isChargingPistol = false
local currentTouchInput = nil
local isAttackCooldown = false
local isPassiveCooldown = false
local lockedTarget = nil
local CustomRemoteHooked = false
local HideSilentLaser = false

-- ==================== CROSSHAIR ====================

local CrosshairEnabled = false

local CrosshairStyle = "Dot"

local CrosshairSize = 8
local CrosshairThickness = 4
local CrosshairGap = 6

local CrosshairPosX = 0
local CrosshairPosY = 0

local CrosshairColor = Color3.fromRGB(255, 255, 0)

local CrosshairObjects = {}

for i = 1, 4 do
    local line = Drawing.new("Line")

    line.Visible = false
    line.Transparency = 1
    line.Thickness = CrosshairThickness
    line.Color = CrosshairColor

    table.insert(CrosshairObjects, line)
end

-- DOT OBJECT
local CrosshairDot = Drawing.new("Circle")

CrosshairDot.Visible = false
CrosshairDot.Filled = true
CrosshairDot.Transparency = 1
CrosshairDot.Color = CrosshairColor
CrosshairDot.Radius = CrosshairSize
CrosshairDot.NumSides = 30

local pistolLaser = Instance.new("Part")
pistolLaser.Name = "VD_PistolLaser"
pistolLaser.Material = Enum.Material.Neon
pistolLaser.Color = Color3.fromRGB(255, 0, 0)
pistolLaser.CanCollide = false
pistolLaser.Anchored = true
pistolLaser.CastShadow = false
pistolLaser.Size = Vector3.new(0.05, 0.05, 1)

local veilTargetHighlight = Instance.new("Highlight")
veilTargetHighlight.Name = "VD_VeilTarget"
veilTargetHighlight.FillColor = Color3.fromRGB(255, 0, 0)
veilTargetHighlight.OutlineColor = Color3.fromRGB(255, 255, 255)
veilTargetHighlight.FillTransparency = 0.5
veilTargetHighlight.OutlineTransparency = 0

local VeilFOVCircle = Drawing.new("Circle")
VeilFOVCircle.Color = Color3.fromRGB(0, 255, 100)
VeilFOVCircle.Thickness = 1.5
VeilFOVCircle.Filled = false

local PistolFOVCircle = Drawing.new("Circle")
PistolFOVCircle.Color = Color3.fromRGB(255, 255, 255)
PistolFOVCircle.Thickness = 1.5
PistolFOVCircle.Filled = false

local function getTargetPartObject(char)
    if AimConfig.AIM_TargetPart == "Head" then 
        return char:FindFirstChild("Head")
    elseif AimConfig.AIM_TargetPart == "Root" then 
        return char:FindFirstChild("HumanoidRootPart")
    else 
        return char:FindFirstChild("Torso") or char:FindFirstChild("UpperTorso") or char:FindFirstChild("HumanoidRootPart") 
    end
end

local function getClosestSurvivor()
    local myChar = player.Character
    local myRoot = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if not myRoot then return nil end
    local closestFovDist = AimConfig.Veil_FOV
    local closestTarget = nil
    local cam = workspace.CurrentCamera
    local centerScreen = Vector2.new(cam.ViewportSize.X / 2, cam.ViewportSize.Y / 2)

    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= player and p.Team and p.Team.Name == "Survivors" and p.Character then
            local char = p.Character
            local hum = char:FindFirstChildOfClass("Humanoid")
            local targetPart = getTargetPartObject(char)
            if hum and hum.Health > 0 and targetPart then
                local dist3D = (targetPart.Position - myRoot.Position).Magnitude
                if dist3D <= AimConfig.SPEAR_MaxDist then
                    local screenPos, onScreen = cam:WorldToViewportPoint(targetPart.Position)
                    if onScreen then
                        local targetPos2D = Vector2.new(screenPos.X, screenPos.Y)
                        local dist2D = (targetPos2D - centerScreen).Magnitude
                        if dist2D <= closestFovDist then
                            closestFovDist = dist2D
                            closestTarget = targetPart
                        end
                    end
                end
            end
        end
    end
    return closestTarget
end

local function getPistolTarget()
    local closestDist = (AimConfig.Pistol_ShowFOV and AimConfig.Pistol_FOVMode) and AimConfig.Pistol_FOV or math.huge
    local bestTarget = nil
    local myChar = player.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if not myHRP then return nil end
    local cam = workspace.CurrentCamera

    if AimConfig.Pistol_Target == "SCP" then
        -- [ MEMBACA CACHE SCP ]
        for _, obj in ipairs(cache.SCPs) do
            if obj and obj.Parent then
                local hum = obj:FindFirstChildOfClass("Humanoid")
                local root = obj:FindFirstChild("HumanoidRootPart") or obj:FindFirstChild("Torso") or obj:FindFirstChild("Head") or obj.PrimaryPart
                if hum and root and hum.Health > 0 and math.floor(hum.WalkSpeed) == 9 then
                    local targetPart = root 
                    if targetPart then
                        local screenPos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
                        if onScreen or not AimConfig.Pistol_FOVMode then
                            local dist = AimConfig.Pistol_FOVMode and (Vector2.new(screenPos.X, screenPos.Y) - centerScreen).Magnitude or (targetPart.Position - myHRP.Position).Magnitude
                            if dist < closestDist then
                                closestDist = dist
                                bestTarget = targetPart
                            end
                        end
                    end
                end
            end
        end
    else
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= player and p.Character then
                local isValidTarget = false
                if AimConfig.Pistol_Target == "Killer" and IsKiller(p) then 
                    isValidTarget = true
                elseif AimConfig.Pistol_Target == "Survivor" and not IsKiller(p) then 
                    isValidTarget = true 
                end
                
                if isValidTarget then
                    local hum = p.Character:FindFirstChildOfClass("Humanoid")
                    if hum and hum.Health > 0 and not IsDowned(p.Character) then
                        local targetPart = getTargetPartObject(p.Character)
                        if targetPart then
                            local screenPos, onScreen = cam:WorldToViewportPoint(targetPart.Position)
                            local mouseLocation = UserInputService:GetMouseLocation()
                            
                            if onScreen or not AimConfig.Pistol_FOVMode then
                                local dist = AimConfig.Pistol_FOVMode 
                                    and (Vector2.new(screenPos.X, screenPos.Y) - mouseLocation).Magnitude 
                                    or (targetPart.Position - myHRP.Position).Magnitude
                                
                                if dist < closestDist then
                                    closestDist = dist
                                    bestTarget = targetPart
                                end
                            end
                        end
                    end
                end
            end
        end
    end
    return bestTarget
end

local function executeSilentAimFire()
    local targetPart = getPistolTarget()
    local myChar = player.Character
    if targetPart and myChar then
        local myHum = myChar:FindFirstChildOfClass("Humanoid")
        if myHum and IsDowned(myChar) then return end
        local myPart = myChar:FindFirstChild("HumanoidRootPart")
        if myPart then
            local twistOfFate = myChar:FindFirstChild("Twist of Fate")
            if twistOfFate then
                local weaponArg = twistOfFate
                local rightArm = twistOfFate:FindFirstChild("Right Arm")
                if rightArm then
                    if rightArm:FindFirstChild("EmperorGun") then 
                        weaponArg = rightArm:FindFirstChild("EmperorGun")
                    elseif rightArm:FindFirstChild("gun") then 
                        weaponArg = rightArm:FindFirstChild("gun")
                    else 
                        weaponArg = rightArm 
                    end
                end
                local leftArm = myChar:FindFirstChild("Left Arm") or myChar:FindFirstChild("LeftHand")
                local startPos = leftArm and leftArm.Position or myPart.Position
                local targetPos = targetPart.Position
                local targetVel = targetPart.AssemblyLinearVelocity
                targetVel = Vector3.new(targetVel.X, 0, targetVel.Z)
                local distance = (targetPos - startPos).Magnitude
                local bulletSpeed = 400
                local timeToHit = distance / bulletSpeed
                local predictedPos = targetPos + (targetVel * timeToHit)
                local offset = Vector3.new(0, -2, 0)
                local aimDirection = ((predictedPos + offset) - startPos).Unit
                local remotes = ReplicatedStorage:FindFirstChild("Remotes")
                if remotes and remotes:FindFirstChild("Items") and remotes.Items:FindFirstChild("Twist of Fate") and remotes.Items["Twist of Fate"]:FindFirstChild("Fire") then
                    remotes.Items["Twist of Fate"].Fire:FireServer(weaponArg, aimDirection)
                end
            end
        end
    end
end

local function setupSpearInterceptor()
    if CustomRemoteHooked then return end
    task.spawn(function()
        pcall(function()
            local oldNamecall
            oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
                local method = getnamecallmethod()
                if method == "FireServer" and not checkcaller() then
                    if self.Name == "Spearthrow" and AimConfig.Aim_SilentVeil then
                        return nil 
                    end
                end
                return oldNamecall(self, ...)
            end)
            CustomRemoteHooked = true
        end)
    end)
end
setupSpearInterceptor()

-- ==================== CROSSHAIR LOOP ====================

RunService.RenderStepped:Connect(function()

    -- HIDE ALL
    for _, obj in pairs(CrosshairObjects) do
        obj.Visible = false
        obj.Thickness = CrosshairThickness
        obj.Color = CrosshairColor
    end

    CrosshairDot.Visible = false
    CrosshairDot.Color = CrosshairColor
    CrosshairDot.Radius = CrosshairSize

    if not CrosshairEnabled then
        return
    end

    local cam = workspace.CurrentCamera

    local centerX = (cam.ViewportSize.X / 2) + CrosshairPosX
    local centerY = (cam.ViewportSize.Y / 2) + CrosshairPosY

    local size = CrosshairSize
    local gap = CrosshairGap

    local left = CrosshairObjects[1]
    local right = CrosshairObjects[2]
    local top = CrosshairObjects[3]
    local bottom = CrosshairObjects[4]

    -- ==================== DOT ====================
    if CrosshairStyle == "Dot" then

        CrosshairDot.Visible = true
        CrosshairDot.Position = Vector2.new(centerX, centerY)
        CrosshairDot.Radius = size
        CrosshairDot.Color = CrosshairColor

    -- ==================== PLUS ====================
    elseif CrosshairStyle == "Plus" then

        left.Visible = true
        left.From = Vector2.new(centerX - gap - size, centerY)
        left.To = Vector2.new(centerX - gap, centerY)

        right.Visible = true
        right.From = Vector2.new(centerX + gap, centerY)
        right.To = Vector2.new(centerX + gap + size, centerY)

        top.Visible = true
        top.From = Vector2.new(centerX, centerY - gap - size)
        top.To = Vector2.new(centerX, centerY - gap)

        bottom.Visible = true
        bottom.From = Vector2.new(centerX, centerY + gap)
        bottom.To = Vector2.new(centerX, centerY + gap + size)

    -- ==================== X ====================
    elseif CrosshairStyle == "X" then

        left.Visible = true
        left.From = Vector2.new(centerX - size, centerY - size)
        left.To = Vector2.new(centerX - gap, centerY - gap)

        right.Visible = true
        right.From = Vector2.new(centerX + gap, centerY + gap)
        right.To = Vector2.new(centerX + size, centerY + size)

        top.Visible = true
        top.From = Vector2.new(centerX + size, centerY - size)
        top.To = Vector2.new(centerX + gap, centerY - gap)

        bottom.Visible = true
        bottom.From = Vector2.new(centerX - gap, centerY + gap)
        bottom.To = Vector2.new(centerX - size, centerY + size)

    -- ==================== BOX ====================
    elseif CrosshairStyle == "Box" then

        left.Visible = true
        left.From = Vector2.new(centerX - size, centerY - size)
        left.To = Vector2.new(centerX + size, centerY - size)

        right.Visible = true
        right.From = Vector2.new(centerX + size, centerY - size)
        right.To = Vector2.new(centerX + size, centerY + size)

        top.Visible = true
        top.From = Vector2.new(centerX + size, centerY + size)
        top.To = Vector2.new(centerX - size, centerY + size)

        bottom.Visible = true
        bottom.From = Vector2.new(centerX - size, centerY + size)
        bottom.To = Vector2.new(centerX - size, centerY - size)
    end
end)

-- ==================== ESP FUNCTIONS ====================
local function ApplyHighlight(object, color, isEnabled)
    if not object then return end
    local h = object:FindFirstChild("H")
    if h and not h:IsA("Highlight") then 
        h:Destroy() 
        h = nil 
    end
    if isEnabled then
        if h then h:Destroy() end
        h = Instance.new("Highlight")
        h.Name = "H"
        h.Adornee = object
        h.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
        h.FillColor = color
        h.OutlineColor = color
        h.FillTransparency = 0.8
        h.OutlineTransparency = 0.3
        h.Parent = object
    else
        if h then h:Destroy() end
    end
end

local function CreateESP(part, text, color, isEnabled, showName, showDist)
    if not part then return end
    local bill = part:FindFirstChild("ESP_TEXT")
    if not bill and isEnabled and text ~= "" then
        bill = Instance.new("BillboardGui")
        bill.Name = "ESP_TEXT"
        bill.Size = UDim2.new(0, 200, 0, 40)
        bill.AlwaysOnTop = true
        bill.StudsOffset = Vector3.new(0, 3, 0)
        bill.Parent = part
        local txt = Instance.new("TextLabel")
        txt.Name = "TextLabel"
        txt.Size = UDim2.new(1, 0, 1, 0)
        txt.BackgroundTransparency = 1
        txt.TextStrokeTransparency = 0
        txt.Font = Enum.Font.GothamBold
        txt.TextSize = 10
        txt.Parent = bill
    end
    if bill then
        bill.Enabled = isEnabled and text ~= ""
        if bill.Enabled then
            local txt = bill:FindFirstChild("TextLabel")
            if txt then
                txt.TextColor3 = color
                local displayText = ""
                if showName then displayText = text end
                if showDist then
                    local myHRP = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
                    if myHRP then
                        local dist = math.floor((myHRP.Position - part.Position).Magnitude)
                        if showName then
                            displayText = text .. " [" .. dist .. "m]"
                        else
                            displayText = "[" .. dist .. "m]"
                        end
                    end
                end
                txt.Text = displayText
            end
        end
    end
end

local function HideAllESP()
    for _, plr in pairs(Players:GetPlayers()) do
        if plr.Character then
            ApplyHighlight(plr.Character, Color3.new(), false)
            local plrHRP = plr.Character:FindFirstChild("HumanoidRootPart")
            if plrHRP then CreateESP(plrHRP, "", Color3.new(), false) end
        end
    end
    for _, data in pairs(cache.Zombie) do
        if data and data.obj then
            ApplyHighlight(data.obj, Color3.new(), false)
            local bill = data.part and data.part:FindFirstChild("ESP_TEXT")
            if bill then bill.Enabled = false end
        end
    end
    local mapItems = {cache.Generator, cache.Hook, cache.Gate, cache.Pallet, cache.Window}
    for _, category in pairs(mapItems) do
        for _, data in pairs(category) do
            if data and data.obj then
                ApplyHighlight(data.obj, Color3.new(), false)
                if data.part then
                    CreateESP(data.part, "", Color3.new(), false)
                end
            end
        end
    end
    local myChar = player.Character
    if myChar and myChar:FindFirstChild("HumanoidRootPart") then
        local warn = myChar.HumanoidRootPart:FindFirstChild("KillerWarn")
        if warn then warn.Enabled = false end
    end
end

local function ShouldShowESP() 
    return not StreamerMode 
end

-- ==================== NEXT KILLER ====================
local IndicatorGui = nil
local function SetupIndicator()
    if IndicatorGui then IndicatorGui:Destroy() end
    IndicatorGui = Instance.new("ScreenGui")
    IndicatorGui.Name = "IndicatorGui"
    local playerGui = player:FindFirstChild("PlayerGui")
    if playerGui then
        IndicatorGui.Parent = playerGui
    else
        player:WaitForChild("PlayerGui")
        IndicatorGui.Parent = player.PlayerGui
    end
    IndicatorGui.ResetOnSpawn = false
end

function StartNextKiller()
    if IndicatorGui then return end
    SetupIndicator()
    task.spawn(function()
        while nextKillerEnabled and IndicatorGui do
            local label = IndicatorGui:FindFirstChild("NextKillerDisplay")
            if not label then
                label = Instance.new("TextLabel")
                label.Name = "NextKillerDisplay"
                label.Size = UDim2.new(0,220,0,30)
                label.Position = UDim2.new(0.5,0,0,45)
                label.AnchorPoint = Vector2.new(0.5,0)
                label.BackgroundTransparency = 0.5
                label.BackgroundColor3 = Color3.new(0,0,0)
                label.TextColor3 = Color3.new(1,1,1)
                label.Font = Enum.Font.GothamBold
                label.TextSize = 14
                label.RichText = true
                label.Parent = IndicatorGui
            end
            local playersList = Players:GetPlayers()
            table.sort(playersList, function(a, b)
                local aA = a:GetAttribute("AllowKiller") or false
                local bA = b:GetAttribute("AllowKiller") or false
                if aA ~= bA then return aA == true end
                return (a:GetAttribute("KillerChance") or 0) > (b:GetAttribute("KillerChance") or 0)
            end)
            local nk = playersList[1]
            if nk then 
                label.Text = "Next Killer: <font color=\"rgb(255,0,0)\">" .. (nk == player and "YOU" or nk.Name) .. "</font>" 
            end
            task.wait(2)
        end
    end)
end

function StopNextKiller()
    if IndicatorGui then 
        IndicatorGui:Destroy() 
        IndicatorGui = nil 
    end
end

-- ==================== PERFORMANCE FUNCTIONS ====================
local Lighting = game:GetService("Lighting")

local function ApplyFullBright()
    if FullBright then
        Lighting.Brightness = 5
        Lighting.ClockTime = TimeOfDayValue
        Lighting.FogEnd = 100000
        Lighting.Ambient = Color3.fromRGB(255,255,255)
        Lighting.OutdoorAmbient = Color3.fromRGB(255,255,255)
    end
end

local function ApplyNoFog()
    if NoFog then
        Lighting.FogStart = 0
        Lighting.FogEnd = 100000
        Lighting.FogColor = Color3.fromRGB(255,255,255)
        local atmosphere = Lighting:FindFirstChildOfClass("Atmosphere")
        if atmosphere then
            atmosphere.Density = 0
            atmosphere.Offset = 0
            atmosphere.Glare = 0
            atmosphere.Haze = 0
        end
    end
end

local function ApplyNoShadow()
    Lighting.GlobalShadows = not NoShadow
end

local function ApplyLowGraphicsToObject(obj)
    if not LowGraphics then return end
    if obj:IsA("BasePart") then
        if not savedGraphics[obj] then
            savedGraphics[obj] = { Material = obj.Material, Reflectance = obj.Reflectance }
        end
        obj.Material = Enum.Material.Plastic
        obj.Reflectance = 0
    elseif obj:IsA("Decal") or obj:IsA("Texture") then
        if not savedGraphics[obj] then
            savedGraphics[obj] = { Transparency = obj.Transparency }
        end
        obj.Transparency = 1
    end
end

local function ApplyFPSBoostToObject(obj)
    if not FPSBoost then return end
    if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Smoke") or obj:IsA("Fire") then
        if savedEffects[obj] == nil then
            savedEffects[obj] = obj.Enabled
        end
        obj.Enabled = false
    end
end

local function ReapplyPerformance()
    task.wait(1)
    ApplyFullBright()
    ApplyNoFog()
    ApplyNoShadow()
    if LowGraphics then
        for _, obj in pairs(workspace:GetDescendants()) do 
            ApplyLowGraphicsToObject(obj) 
        end
    end
    if FPSBoost then
        for _, obj in pairs(workspace:GetDescendants()) do 
            ApplyFPSBoostToObject(obj) 
        end
    end
end

-- ==================== EMOTE FUNCTIONS ====================
local function PlayEmote()
    local char = player.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hum or not hrp then return end
    if currentTrack then currentTrack:Stop() end
    if currentSound then currentSound:Destroy() end
    if SelectedAnim then
        local anim = Instance.new("Animation")
        anim.AnimationId = SelectedAnim
        currentTrack = hum:LoadAnimation(anim)
        currentTrack.Looped = true
        currentTrack:Play()
    end
    if SelectedSound then
        currentSound = Instance.new("Sound")
        currentSound.SoundId = SelectedSound
        currentSound.Looped = true
        currentSound.Volume = 2
        currentSound.Parent = hrp
        currentSound:Play()
    end
end

local function StopEmote()
    if currentTrack then currentTrack:Stop() end
    if currentSound then currentSound:Destroy() end
end

-- ==================== SPEED BOOST FUNCTIONS ====================
local function startSpeedInputMode()
    if SpeedInputConnection then return end
    SpeedInputConnection = RunService.RenderStepped:Connect(function()
        if not SpeedInputEnabled or not player.Character then return end
        local hum = player.Character:FindFirstChild("Humanoid")
        local hrp = player.Character:FindFirstChild("HumanoidRootPart")
        if not hum or not hrp then return end
        local moveDir = hum.MoveDirection
        if moveDir.Magnitude > 0 then
            hrp.CFrame = hrp.CFrame + (moveDir * SpeedInputValue)
        end
    end)
end

local function stopSpeedInputMode()
    if SpeedInputConnection then
        SpeedInputConnection:Disconnect()
        SpeedInputConnection = nil
    end
end

-- ==================== BYPASS GATE ====================
local function gatherGates()
    local gates = {}

    local map = workspace:FindFirstChild("Map")
    if not map then
        warn("Map not found")
        return gates
    end

    for _, obj in pairs(map:GetDescendants()) do
        if obj.Name == "Gate" then
            table.insert(gates, obj)
        end
    end

    return gates
end

local function setBypassGate(state)
    BypassGateEnabled = state

    local gates = gatherGates()

    for _, gate in pairs(gates) do
        local leftGate = gate:FindFirstChild("LeftGate")
        local rightGate = gate:FindFirstChild("RightGate")

        local leftEnd =
            gate:FindFirstChild("LeftGate-end") or
            gate:FindFirstChild("LeftGate-end2")

        local rightEnd =
            gate:FindFirstChild("RightGate-end") or
            gate:FindFirstChild("RightGate-end2")

        local box = gate:FindFirstChild("Box")

        if state then
            if leftGate then
                leftGate.Transparency = 1
                leftGate.CanCollide = false
            end

            if rightGate then
                rightGate.Transparency = 1
                rightGate.CanCollide = false
            end

            if leftEnd then
                leftEnd.Transparency = 0
            end

            if rightEnd then
                rightEnd.Transparency = 0
            end

            if box then
                box.CanCollide = false
            end

        else
            if leftGate then
                leftGate.Transparency = 0
                leftGate.CanCollide = true
            end

            if rightGate then
                rightGate.Transparency = 0
                rightGate.CanCollide = true
            end

            if leftEnd then
                leftEnd.Transparency = 1
            end

            if rightEnd then
                rightEnd.Transparency = 1
            end

            if box then
                box.CanCollide = true
            end
        end
    end
end

--// ===== FORCE JUMP FUNCTIONS START =====

local function DisableForceJump()
    ForceJumpEnabled = false

    if ForceJumpHeartbeat then
        ForceJumpHeartbeat:Disconnect()
        ForceJumpHeartbeat = nil
    end

    if ForceJumpInput then
        ForceJumpInput:Disconnect()
        ForceJumpInput = nil
    end

    if ForceJumpCharacter then
        ForceJumpCharacter:Disconnect()
        ForceJumpCharacter = nil
    end
end

local function SetupForceJump(Character)
    local Humanoid = Character:FindFirstChild("Humanoid")
    if not Humanoid then return end

    Humanoid.JumpPower = 50

    if ForceJumpHeartbeat then
        ForceJumpHeartbeat:Disconnect()
    end

    ForceJumpHeartbeat = RunService.Heartbeat:Connect(function()
        if not ForceJumpEnabled then return end

        local Char = LocalPlayer.Character
        local Hum = Char and Char:FindFirstChild("Humanoid")

        if Hum and Hum.JumpPower == 0 then
            Hum.JumpPower = 50
        end
    end)
end

local function EnableForceJump()
    DisableForceJump()

    ForceJumpEnabled = true

    local Character = LocalPlayer.Character
    if Character then
        SetupForceJump(Character)
    end

    ForceJumpCharacter = LocalPlayer.CharacterAdded:Connect(function(Char)
        task.wait(1)

        if ForceJumpEnabled then
            SetupForceJump(Char)
        end
    end)

    ForceJumpInput = UserInputService.InputBegan:Connect(function(Input, GPE)
        if not ForceJumpEnabled or GPE then return end

        if Input.KeyCode == Enum.KeyCode.Space then
            local Char = LocalPlayer.Character
            local Hum = Char and Char:FindFirstChild("Humanoid")

            if Hum then
                Hum:ChangeState(Enum.HumanoidStateType.Jumping)
            end
        end
    end)
end

--// ===== FORCE JUMP FUNCTIONS END =====

-- ===== FLEE KILLER FUNCTIONS START =====

local function GetNearestKiller()
    local root = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
    if not root then
        return nil, math.huge
    end

    local nearestKiller = nil
    local nearestDistance = math.huge

    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= player
        and plr.Team
        and plr.Team.Name == "Killer"
        and plr.Character
        and plr.Character:FindFirstChild("HumanoidRootPart") then

            local dist = (plr.Character.HumanoidRootPart.Position - root.Position).Magnitude

            if dist < nearestDistance then
                nearestDistance = dist
                nearestKiller = plr
            end
        end
    end

    return nearestKiller, nearestDistance
end

local function FleeKiller()
    if not FleeKillerEnabled then
        return
    end

    local root = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
    if not root then
        return
    end

    local killer, distance = GetNearestKiller()

    if not killer or distance > FleeDistance then
        return
    end

    if tick() - FleeCooldown < 3 then
        return
    end

    local killerRoot = killer.Character and killer.Character:FindFirstChild("HumanoidRootPart")
    if not killerRoot then
        return
    end

    local direction = (root.Position - killerRoot.Position).Unit
    local safePosition = root.Position + direction * 80

    root.CFrame = CFrame.new(safePosition)

    FleeCooldown = tick()
end

-- ===== FLEE KILLER FUNCTIONS END =====

-- ==================== OBSIDIAN UI SETUP (deividcomsono version) ====================
local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

local Options = Library.Options
local Toggles = Library.Toggles

Library.ForceCheckbox = false
Library.ShowToggleFrameInKeybinds = true

local Window = Library:CreateWindow({
    Title = "Pandu Hub",
    Footer = 'Made By Pandu - Violence District v1.2.0',
    Icon = 109651670989001,
    IconSize = UDim2.fromOffset(50, 50),
    NotifySide = "Right",
    EnableSidebarResize = true,
    EnableCompacting = true,
    SidebarCompacted = true,
    Size = UDim2.fromOffset(450, 1000),
    CornerRadius = 10,
    AutoShow = true,
})

local TabsUI = {
    Main = Window:AddTab("Pandu", "crown"),
    Survivor = Window:AddTab("Survivor", "users"),
    Killer = Window:AddTab("Killer", "swords"),
    Esp = Window:AddTab("ESP", "eye"),
    Teleport = Window:AddTab("Teleport", "map-pin"),
    Misc = Window:AddTab("Misc", "settings"),
    ["UI Settings"] = Window:AddTab("UI Settings", "files"),
}

-- ==================== TAB MAIN ====================
local MainGroup = TabsUI.Main:AddLeftGroupbox("Tentang Pandu Hub", "info")

MainGroup:AddLabel("Pandu Hub adalah script hub yang ringan, stabil, dan gampang dipake.\n\nDibuat buat bantu gameplay jadi lebih enak, dengan fitur otomatis, tampilan clean, dan kontrol yang responsif.\n\nFokus ke performa, kecepatan, dan kemudahan dipake di berbagai situasi.", true)

-- Discord Section
local DiscordGroup = TabsUI.Main:AddRightGroupbox("Discord Server", "message-circle")
local InviteCode = "AzauctDu4"

DiscordGroup:AddButton({
    Text = "Copy Invite Link",
    Func = function()
        if setclipboard then
            setclipboard("https://discord.gg/" .. InviteCode)
            Library:Notify({ Title = "Discord", Description = "Link Discord udah ke-copy!", Time = 5 })
        else
            Library:Notify({ Title = "Gagal", Description = "setclipboard tidak tersedia di executor ini.", Time = 4 })
        end
    end,
})

DiscordGroup:AddButton({
    Text = "Join Discord",
    Func = function()
        Library:Notify({ Title = "Discord", Description = "Buka browser lalu masuk ke:\nhttps://discord.gg/" .. InviteCode, Time = 6 })
    end,
})

-- Auto Parry
local ParryGroup = TabsUI.Survivor:AddLeftGroupbox("Ability", "swords")

ParryGroup:AddToggle("AutoSkillCheck", {
    Text = "Auto Skill Check Gen",
    Tooltip = "Auto perfect skill check Gen",
    Default = false,
    Callback = function(Value)
        AutoSkillCheckEnabled = Value
    end,
})
ParryGroup:AddToggle("Surv_AutoCrouch", {
    Text = "Auto Crouch (Dodge S1)",
    Tooltip = "Otomatis jongkok saat Abyssal menggunakan S1",
    Default = false,
    Callback = function(Value)
        Config.Surv_AutoCrouch = Value
    end,
})

ParryGroup:AddToggle("Surv_InstanSkillCheck", {
    Text = "Instan Skill Check",
    Tooltip = "Auto perfect skillcheck instan (mengganti bawaan game)",
    Default = false,
    Callback = function(Value)
        Config.Surv_InstanSkillCheck = Value
        getgenv().InstanSkillCheck_Active = Value
        
        if Value then
            if not CachedInstnScript then
                local success, result = pcall(function()
                    return game:HttpGet("https://raw.githubusercontent.com/tiooxy/rosblox/main/Misc/Instn")
                end)
                if success and result then
                    CachedInstnScript = result
                else
                    Library:Notify({Title = "Error", Description = "Gagal mendownload Instn Script!", Time = 3})
                    return
                end
            end
            
            pcall(function() loadstring(CachedInstnScript)() end)
            Library:Notify({Title = "Sukses", Description = "Instan Skill Check Aktif!", Time = 3})
            
            task.spawn(function()
                local char = LocalPlayer.Character
                if char then
                    local genScript = char:WaitForChild("Skillcheck-gen", 3)
                    local playerScript = char:WaitForChild("Skillcheck-player", 3)
                    if genScript then genScript.Disabled = true end
                    if playerScript then playerScript.Disabled = true end
                end
            end)
        else
            if getgenv().RemoveInstanSkillCheck then
                pcall(getgenv().RemoveInstanSkillCheck)
            end
            local char = LocalPlayer.Character
            if char then
                local genScript = char:FindFirstChild("Skillcheck-gen")
                local playerScript = char:FindFirstChild("Skillcheck-player")
                if genScript then genScript.Disabled = false end
                if playerScript then playerScript.Disabled = false end
            end
        end
    end
})
ParryGroup:AddSlider("SkillFrequency", {
    Text = "Skill Check Frequency",
    Tooltip = "Atur frekuensi munculnya skill check",
    Default = 10,
    Min = 1,
    Max = 50,
    Rounding = 0,
    Callback = function(Value)
        ConfigData.Surv_SkillFrequency = Value
    end,
})

ParryGroup:AddSlider("SkillSpeed", {
    Text = "Skill Check Speed",
    Tooltip = "Atur kecepatan putaran skill check (1-30, 10 = Normal)",
    Default = 10,
    Min = 1,
    Max = 30,
    Rounding = 1,
    Callback = function(Value)
        ConfigData.Surv_SkillSpeed = Value / 10
    end,
})

ParryGroup:AddToggle("BypassGate", {
    Text = "Bypass Exite Gate",
    Tooltip = "Tembus gate tanpa collision",
    Default = false,
    Callback = function(Value)
        setBypassGate(Value)

        if Value then
            Library:Notify({
                Title = "Bypass Exite Gate",
                Description = "Gate bypass aktif!",
                Time = 3
            })
        else
            Library:Notify({
                Title = "Bypass Exite Gate",
                Description = "Gate bypass dimatikan!",
                Time = 3
            })
        end
    end,
})

ParryGroup:AddToggle("GodMode", {
    Text = "Anti Knockdown",
    Tooltip = "Ga bisa mati (semi god)",
    Default = false,
    Callback = function(Value)
        GodMode = Value
    end,
})

ParryGroup:AddToggle("AutoWiggle", {
    Text = "Auto Wiggle",
    Tooltip = "Biar lebih cepet lepas dari carry killer",
    Default = false,
    Callback = function(Value)
        AutoWiggleEnabled = Value
    end,
})


ParryGroup:AddToggle("FleeKiller", {
    Text = "Flee Killer",
    Tooltip = "Teleport saat killer terlalu dekat",
    Default = false,

    Callback = function(Value)
        FleeKillerEnabled = Value
    end,
})

ParryGroup:AddSlider("FleeDistance", {
    Text = "Flee Distance",
    Tooltip = "Jarak trigger teleport",
    Default = 40,
    Min = 15,
    Max = 80,
    Rounding = 0,

    Callback = function(Value)
        FleeDistance = Value
    end,
})

ParryGroup:AddDivider()

ParryGroup:AddToggle("AutoParry", {
    Text = "Auto Parry",
    Tooltip = "Otomatis menangkis serangan killer",
    Default = false,
    Callback = function(Value)
        Config.Surv_AutoParry = Value
    end,
})

ParryGroup:AddToggle("ParryAggressive", {
    Text = "Aggressive Mode",
    Tooltip = "Langsung parry tanpa peduli face direction",
    Default = false,
    Callback = function(Value)
        Config.Surv_ParryAggressive = Value
    end,
})

ParryGroup:AddToggle("ParryCircle", {
    Text = "ESP Range Circle",
    Tooltip = "Tampilkan radius jarak parry di karakter",
    Default = true,
    Callback = function(Value)
        Config.Surv_ParryCircle = Value
    end,
})

ParryGroup:AddSlider("ParryRadius", {
    Text = "Parry Radius",
    Tooltip = "Jarak maksimal parry bereaksi",
    Default = 15,
    Min = 5,
    Max = 25,
    Rounding = 0,
    Callback = function(Value)
        Config.Surv_ParryRadius = Value
    end,
})

ParryGroup:AddSlider("ParryFace", {
    Text = "Face Sensitivity",
    Tooltip = "Sensitivitas arah pandang (1-10)",
    Default = 7,
    Min = -10,
    Max = 10,
    Rounding = 0,
    Callback = function(Value)
        Config.Surv_ParryFace = Value / 10
    end,
})

ParryGroup:AddDropdown("IgnoreSkills", {
    Values = { "Hidden S1", "Abyssal S1" },
    Default = {},
    Multi = true,
    Text = "Abaikan skill tertentu",
    Tooltip = "Abaikan skill tertentu",
    Callback = function(Value)
        local parsed = {}
        for k, v in pairs(Value) do
            if type(k) == "string" and v then 
                parsed[k] = true
            elseif type(v) == "string" then 
                parsed[v] = true 
            end
        end
        Config.Ignored_Skills_List = parsed
    end,
})

local AgilityGroup = TabsUI.Survivor:AddLeftGroupbox("Agility", "zap")

AgilityGroup:AddButton({
    Text = "Force Jump",

    Callback = function()
        EnableForceJump()

        Library:Notify({
            Title = "Force Jump",
            Description = "Enabled",
            Time = 3
        })
    end
})

AgilityGroup:AddToggle("InfiniteJump", {
    Text = "Infinite Jump",
    Tooltip = "Lompat tanpa batas",
    Default = false,
    Callback = function(Value)
        if Value then
            local conn
            conn = game:GetService("UserInputService").JumpRequest:Connect(function()
                pcall(function()
                    if player.Character and player.Character:FindFirstChild("Humanoid") then
                        player.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                    end
                end)
            end)
        end
    end,
})

-- ==================== TAB SURVIVOR ====================
-- Auto Farm & Webhook
local FarmGroup = TabsUI.Survivor:AddRightGroupbox("Automatic", "bot")

FarmGroup:AddToggle("AutoAFKEscape", {
    Text = "AFK Auto Escape & Hop",
    Tooltip = "Auto Farm survivor - teleport ke exit dan server hop",
    Default = false,
    Callback = function(Value)
        Config.AUTO_AFKEscape = Value
        if Value then
            HandleAFKLogic(LocalPlayer.Team and LocalPlayer.Team.Name or "Unknown")
        else
            if AFK_Thread then 
                task.cancel(AFK_Thread) 
                AFK_Thread = nil
            end
        end
    end,
})

FarmGroup:AddToggle("WebhookEnabled", {
    Text = "Aktifkan Webhook",
    Tooltip = "Kirim statistik auto farm ke Discord",
    Default = false,
    Callback = function(Value)
        Config.Webhook_Enabled = Value
    end,
})

FarmGroup:AddInput("WebhookURL", {
    Text = "Webhook URL",
    Tooltip = "Masukkan link webhook Discord kesini",
    Default = "",
    Placeholder = "https://discord.com/api/webhooks/...",
    Callback = function(Value)
        if Value ~= nil and Value ~= "" then
            Config.Webhook_URL = Value
        end
    end,
})

FarmGroup:AddButton({
    Text = "Test Webhook",
    Func = function()
        if Config.Webhook_URL == "" then
            Library:Notify({ Title = "Error", Description = "Isi URL Webhook terlebih dahulu!", Time = 3 })
            return
        end
        local tempStatus = Config.Webhook_Enabled
        Config.Webhook_Enabled = true
        SendWebhook()
        Config.Webhook_Enabled = tempStatus
        Library:Notify({ Title = "Sukses", Description = "Pesan test telah dikirim!", Time = 3 })
    end,
})

local PistolGroup = TabsUI.Survivor:AddRightGroupbox("Silent Aim (Pistol)", "crosshair")

PistolGroup:AddToggle("SilentAim", {
    Text = "Silent Aim (Pistol)",
    Tooltip = "Auto aim untuk item Pistol",
    Default = false,
    Callback = function(Value)
        AimConfig.Aim_Silent = Value
    end,
})

PistolGroup:AddToggle("LockAim", {
    Text = "Lock Aim (Pistol)",
    Tooltip = "Lock aim untuk item Pistol",
    Default = false,
    Callback = function(Value)
        AimConfig.LockAim = Value
    end,
})

PistolGroup:AddToggle("HideSilentLaser", {
    Text = "Hide Silent Laser",
    Tooltip = "Sembunyikan laser merah silent aim",
    Default = false,
    Callback = function(Value)
        HideSilentLaser = Value
        
        if Value and pistolLaser then
            pistolLaser.Transparency = 1
        elseif pistolLaser then
            pistolLaser.Transparency = 0
        end
    end,
})

PistolGroup:AddButton({
    Text = "Killer (K)",
    Func = function()
        AimConfig.Pistol_Target = "Killer"
        Library:Notify("Target: Killer", 1)
    end
})

PistolGroup:AddButton({
    Text = "Survivor (J)",
    Func = function()
        AimConfig.Pistol_Target = "Survivor"
        Library:Notify("Target: Survivor", 1)
    end
})

PistolGroup:AddButton({
    Text = "SCP (L)",
    Func = function()
        AimConfig.Pistol_Target = "SCP"
        Library:Notify("Target: SCP", 1)
    end
})

-- Keybinds
UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == Enum.KeyCode.K then
        AimConfig.Pistol_Target = "Killer"
        Library:Notify("Target: Killer", 1)
    elseif input.KeyCode == Enum.KeyCode.J then
        AimConfig.Pistol_Target = "Survivor"
        Library:Notify("Target: Survivor", 1)
    elseif input.KeyCode == Enum.KeyCode.L then
        AimConfig.Pistol_Target = "SCP"
        Library:Notify("Target: SCP", 1)
    end
end)

PistolGroup:AddToggle("PistolFOVMode", {
    Text = "Pistol FOV Mode",
    Default = false,
    Callback = function(Value)
        AimConfig.Pistol_FOVMode = Value
    end,
})


PistolGroup:AddToggle("PistolShowFOV", {
    Text = "Show Pistol FOV",
    Default = false,
    Callback = function(Value)
        AimConfig.Pistol_ShowFOV = Value
    end,
})

PistolGroup:AddSlider("PistolFOV", {
    Text = "Pistol FOV Radius",
    Default = 150,
    Min = 50,
    Max = 500,
    Rounding = 0,
    Callback = function(Value)
        AimConfig.Pistol_FOV = Value
    end,
})

PistolGroup:AddDropdown("AimTargetPart", {
    Values = { "Head", "Torso", "Root" },
    Default = "Torso",
    Text = "Target Part",
    Callback = function(Value)
        AimConfig.AIM_TargetPart = Value
    end,
})

local SurvivorMainGroup = TabsUI.Survivor:AddRightGroupbox("Aimbot Survivor", "target")

SurvivorMainGroup:AddToggle("Surv_Aimbot_Enabled", { Text = "Enable Aimbot", Default = false, Callback = function(Value) Config.Surv_Aimbot_Enabled = Value end })
SurvivorMainGroup:AddCheckbox("Surv_Aimbot_ShowFOV", { Text = "Show FOV Circle", Default = true, Callback = function(Value) Config.Surv_Aimbot_ShowFOV = Value end })
SurvivorMainGroup:AddSlider("Surv_Aimbot_Radius", { Text = "FOV Circle Radius", Min = 50, Max = 500, Default = 150, Rounding = 0, Callback = function(Value) Config.Surv_Aimbot_Radius = Value end })
SurvivorMainGroup:AddSlider("Surv_Aimbot_MaxDist", { Text = "Max Distance", Min = 50, Max = 1000, Default = 300, Rounding = 0, Callback = function(Value) Config.Surv_Aimbot_MaxDist = Value end })
SurvivorMainGroup:AddSlider("Surv_Aimbot_Smoothness", { Text = "Aimbot Smoothness", Min = 1, Max = 10, Default = 5, Rounding = 1, Callback = function(Value) Config.Surv_Aimbot_Smoothness = Value / 10 end })
SurvivorMainGroup:AddInput("Surv_Aimbot_Predict", { Text = "Predict Aim Offset", Default = "0.01", Numeric = true, Placeholder = "misal: 0.05", Callback = function(Value) local num = tonumber(Value); if num then Config.Surv_Aimbot_Predict = num end end })

-- Camera Settings
local CameraGroup = TabsUI.Survivor:AddRightGroupbox("Camera Settings", "camera")

CameraGroup:AddToggle("InfinityZoom", {
    Text = "Infinity Zoom Out",
    Tooltip = "Zoom Out tanpa batas",
    Default = false,
    Callback = function(Value)
        if Value then
            player.CameraMaxZoomDistance = math.huge
            player.CameraMinZoomDistance = 0
        else
            player.CameraMaxZoomDistance = 12
            player.CameraMinZoomDistance = 0.5
        end
    end,
})

local FOVEnabled = false
local TargetFOV = 90

local function tweenFOV(value)
    if not Camera then return end
    TweenService:Create(
        Camera,
        TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
        {FieldOfView = value}
    ):Play()
end

CameraGroup:AddToggle("CameraFOV", {
    Text = "Camera FOV",
    Tooltip = "Atur jarak pandang kamera",
    Default = false,
    Callback = function(Value)
        FOVEnabled = Value
        if Value then
            tweenFOV(TargetFOV)
        else
            tweenFOV(70)
        end
    end,
})

CameraGroup:AddSlider("FOVValue", {
    Text = "FOV Value",
    Default = 90,
    Min = 60,
    Max = 120,
    Rounding = 0,
    Callback = function(Value)
        TargetFOV = Value
        if FOVEnabled and Camera then
            Camera.FieldOfView = Value
        end
    end,
})

-- ==================== TAB KILLER ====================
local KillerCombatGroup = TabsUI.Killer:AddLeftGroupbox("Ability", "swords")

local AutoKillAllEnabled = false
local KillRange = 500
local AttackDelay = 0.45

KillerCombatGroup:AddToggle("Killer_Aimbot_Enabled", { Text = "Enable Aimbot", Default = false, Callback = function(Value) Config.Killer_Aimbot_Enabled = Value end })
KillerCombatGroup:AddSlider("Killer_Aimbot_MaxDist", { Text = "Melee Lock Distance", Min = 5, Max = 30, Default = 12, Rounding = 0, Callback = function(Value) Config.Killer_Aimbot_MaxDist = Value end })
KillerCombatGroup:AddSlider("Killer_Aimbot_Smoothness", { Text = "Aimbot Smoothness", Min = 1, Max = 10, Default = 5, Rounding = 1, Callback = function(Value) Config.Killer_Aimbot_Smoothness = Value / 10 end })

KillerCombatGroup:AddToggle("AutoKillAll", {
    Text = "Auto Kill All",
    Tooltip = "Automatically hunt and eliminate all survivors",
    Default = false,
    Callback = function(Value)
        AutoKillAllEnabled = Value
        if Value then
            Library:Notify({ Title = "Warning!", Description = "Fitur berisiko! Auto Kill All aktif", Time = 3 })
        end
    end,
})

KillerCombatGroup:AddToggle("AutoAttack", {
    Text = "Auto Attack",
    Tooltip = "Auto hit survi yang deket",
    Default = false,
    Callback = function(Value)
        AutoAttackEnabled = Value
    end,
})

KillerCombatGroup:AddToggle("AutoCarry", {
    Text = "Auto Carry & Hook",
    Tooltip = "Otomatis angkut survivor yang down ke hook",
    Default = false,
    Callback = function(Value)
        AutoCarryEnabled = Value
    end,
})

KillerCombatGroup:AddButton({
    Text = "Open Mask Selector GUI",
    Func = function()
        local success, err = pcall(function()
            loadstring(game:HttpGet("https://pastefy.app/nJrAelfC/raw"))()
        end)
        if success then
            Library:Notify({ Title = "Mask Selector", Description = "GUI Mask Selector berhasil dimuat!", Time = 3 })
        else
            Library:Notify({ Title = "Error", Description = "Gagal memuat Mask Selector!", Time = 3 })
        end
    end
})

local VeilGroup = TabsUI.Killer:AddRightGroupbox("Silent Aim (Veil Spear)", "arrow-right")

VeilGroup:AddToggle("SilentAimVeil", {
    Text = "Silent Aim (Veil Spear)",
    Tooltip = "Auto aim spear untuk Veil",
    Default = false,
    Callback = function(Value)
        AimConfig.Aim_SilentVeil = Value
    end,
})
VeilGroup:AddToggle("SilentAimPredict", {
    Text = "Auto Predict",
    Tooltip = "Auto predict spear untuk Veil",
    Default = false,
    Callback = function(Value)
        AimConfig.AIM_Auto = Value
    end,
})

VeilGroup:AddToggle("VeilShowFOV", {
    Text = "Show Veil FOV",
    Default = true,
    Callback = function(Value)
        AimConfig.Veil_ShowFOV = Value
    end,
})

VeilGroup:AddSlider("VeilFOV", {
    Text = "Veil FOV Radius",
    Default = 150,
    Min = 50,
    Max = 500,
    Rounding = 0,
    Callback = function(Value)
        AimConfig.Veil_FOV = Value
    end,
})

-- Killer Camera
local KillerCamGroup = TabsUI.Killer:AddRightGroupbox("Camera Setting", "camera")

KillerCamGroup:AddToggle("KillerThirdPerson", {
    Text = "Third Person (Killer)",
    Tooltip = "Mengubah posisi kamera ke belakang karakter",
    Default = false,
    Callback = function(Value)
        local isPlayerKiller = (player.Team and player.Team.Name == "Killer")
        
        if Value and not isPlayerKiller then
            Library:Notify({ Title = "Akses Ditolak", Description = "Fitur ini khusus Killer!", Time = 3 })
            return
        end

        Config.Killer_3rdPerson = Value
        
        if not Value then
            if State.ThirdPersonConn then
                State.ThirdPersonConn:Disconnect()
                State.ThirdPersonConn = nil
            end
            
            if isPlayerKiller then
                player.CameraMode = Enum.CameraMode.LockFirstPerson
                player.CameraMinZoomDistance = 0.5
                player.CameraMaxZoomDistance = 0.5
            else
                player.CameraMode = Enum.CameraMode.Classic
                player.CameraMinZoomDistance = 0.5
                player.CameraMaxZoomDistance = 128
            end
            
            for _, obj in pairs(Camera:GetChildren()) do
                if obj ~= player.Character then
                    for _, part in pairs(obj:GetDescendants()) do
                        if part:IsA("BasePart") or part:IsA("Decal") then
                            part.LocalTransparencyModifier = 0
                            part.Transparency = 0 
                        end
                    end
                end
            end
            
            if player.Character then
                for _, obj in pairs(player.Character:GetChildren()) do
                    if obj.Name == "Hat" and obj:GetAttribute("loc") == "Head" then
                        for _, child in pairs(obj:GetDescendants()) do
                            if child:IsA("BasePart") or child:IsA("Decal") then
                                child.LocalTransparencyModifier = 1
                            end
                        end
                    end
                    if obj:IsA("BasePart") and armParts[obj.Name] then
                        obj.LocalTransparencyModifier = 1
                    end
                    if isWeapon(obj) then
                        for _, child in pairs(obj:IsA("BasePart") and {obj} or obj:GetDescendants()) do
                            if child:IsA("BasePart") or child:IsA("Decal") then
                                child.LocalTransparencyModifier = 1
                                child.Transparency = 1
                            end
                        end
                    end
                end
            end
        else
            player.CameraMode = Enum.CameraMode.Classic
            player.CameraMinZoomDistance = 7
            player.CameraMaxZoomDistance = 128
            
            State.ThirdPersonConn = RunService.RenderStepped:Connect(function()
                if not Config.Killer_3rdPerson then return end
                
                if player.Team and player.Team.Name ~= "Killer" then
                    return
                end
                
                player.CameraMode = Enum.CameraMode.Classic
                player.CameraMinZoomDistance = 7
                player.CameraMaxZoomDistance = 128
                
                for _, obj in pairs(Camera:GetChildren()) do
                    if obj ~= player.Character then
                        for _, part in pairs(obj:GetDescendants()) do
                            if part:IsA("BasePart") or part:IsA("Decal") then
                                part.LocalTransparencyModifier = 1
                                part.Transparency = 1
                            end
                        end
                    end
                end
                
                if player.Character then
                    for _, obj in pairs(player.Character:GetChildren()) do
                        if obj.Name == "Hat" then
                            for _, child in pairs(obj:GetDescendants()) do
                                if child:IsA("BasePart") or child:IsA("Decal") then
                                    child.LocalTransparencyModifier = 0
                                    child.Transparency = 0
                                end
                            end
                        end
                        if obj:IsA("BasePart") and armParts[obj.Name] then
                            obj.LocalTransparencyModifier = 0
                            obj.Transparency = 0
                        end
                        if isWeapon(obj) then
                            for _, child in pairs(obj:IsA("BasePart") and {obj} or obj:GetDescendants()) do
                                if child:IsA("BasePart") or child:IsA("Decal") then
                                    child.LocalTransparencyModifier = 0
                                    child.Transparency = 0
                                end
                            end
                        end
                    end
                end
            end)
        end
    end,
})

-- ==================== TAB ESP ====================
local EspPlayerGroup = TabsUI.Esp:AddLeftGroupbox("ESP Player", "users")

local SurvivorESP = EspPlayerGroup:AddToggle("ESPSurvivor", {
    Text = "ESP Survivor",
    Tooltip = "Lihat semua posisi survivor",
    Default = false,
    Callback = function(Value)
        espSurvivor = Value
    end,
})

SurvivorESP:AddColorPicker("SurvivorESPColor", {
    Default = SurvivorESPColor,
    Title = "Survivor Color",
    Transparency = 0,
    Callback = function(Value)
        SurvivorESPColor = Value
    end
})

local KillerESP = EspPlayerGroup:AddToggle("ESPKiller", {
    Text = "ESP Killer",
    Tooltip = "Lihat posisi killer",
    Default = false,
    Callback = function(Value)
        espKiller = Value
    end,
})

KillerESP:AddColorPicker("KillerESPColor", {
    Default = KillerESPColor,
    Title = "Killer Color",
    Transparency = 0,
    Callback = function(Value)
        KillerESPColor = Value
    end
})

local ZombieESP = EspPlayerGroup:AddToggle("ESPZombie", {
    Text = "ESP Zombie (SCP)",
    Tooltip = "Tampilkan semua SCP Zombie",
    Default = false,
    Callback = function(Value)
        espZombie = Value
    end,
})

ZombieESP:AddColorPicker("ZombieESPColor", {
    Default = ZombieESPColor,
    Title = "Zombie Color",
    Transparency = 0,
    Callback = function(Value)
        ZombieESPColor = Value
    end
})

EspPlayerGroup:AddDivider()

EspPlayerGroup:AddToggle("ESPName", {
    Text = "ESP Name",
    Tooltip = "Tampilkan nama player",
    Default = true,
    Callback = function(Value)
        espName = Value
    end,
})

EspPlayerGroup:AddToggle("ESPDistance", {
    Text = "ESP Distance",
    Tooltip = "Tampilkan jarak player",
    Default = true,
    Callback = function(Value)
        espDistance = Value
    end,
})

EspPlayerGroup:AddDivider()

EspPlayerGroup:AddToggle("KillerWarn", {
    Text = "Killer Warn",
    Tooltip = "Kasih peringatan berupa (!!) kalau killer dekat",
    Default = false,
    Callback = function(Value)
        killerWarnEnabled = Value
    end,
})

EspPlayerGroup:AddToggle("NextKiller", {
    Text = "Next Killer Display",
    Tooltip = "Prediksi killer selanjutnya",
    Default = false,
    Callback = function(Value)
        nextKillerEnabled = Value
        if Value then 
            StartNextKiller() 
        else 
            StopNextKiller() 
        end
    end,
})

-- ESP Map
local EspMapGroup = TabsUI.Esp:AddLeftGroupbox("ESP Map", "map")

local GeneratorESP = EspMapGroup:AddToggle("ESPGen", {
    Text = "ESP Generator",
    Tooltip = "Tampilkan semua lokasi generator",
    Default = false,
    Callback = function(Value)
        espGen = Value
    end,
})

GeneratorESP:AddColorPicker("GeneratorESPColor", {
    Default = GeneratorESPColor,
    Title = "Generator Color",
    Transparency = 0,
    Callback = function(Value)
        GeneratorESPColor = Value
    end
})

local HookESP = EspMapGroup:AddToggle("ESPHook", {
    Text = "ESP Hook",
    Tooltip = "Tampilkan semua lokasi hook",
    Default = false,
    Callback = function(Value)
        espHook = Value
    end,
})

HookESP:AddColorPicker("HookESPColor", {
    Default = HookESPColor,
    Title = "Hook Color",
    Transparency = 0,
    Callback = function(Value)
        HookESPColor = Value
    end
})

local GateESP = EspMapGroup:AddToggle("ESPGate", {
    Text = "ESP Gate",
    Tooltip = "Tampilkan semua gate/pintu keluar",
    Default = false,
    Callback = function(Value)
        espGate = Value
    end,
})

GateESP:AddColorPicker("GateESPColor", {
    Default = GateESPColor,
    Title = "Gate Color",
    Transparency = 0,
    Callback = function(Value)
        GateESPColor = Value
    end
})

local PalletESP = EspMapGroup:AddToggle("ESPPallet", {
    Text = "ESP Pallet",
    Tooltip = "Tampilkan semua pallet",
    Default = false,
    Callback = function(Value)
        espPallet = Value
    end,
})

PalletESP:AddColorPicker("PalletESPColor", {
    Default = PalletESPColor,
    Title = "Pallet Color",
    Transparency = 0,
    Callback = function(Value)
        PalletESPColor = Value
    end
})

local WindowESP = EspMapGroup:AddToggle("ESPWindow", {
    Text = "ESP Window",
    Tooltip = "Tampilkan semua window/jendela",
    Default = false,
    Callback = function(Value)
        espWindow = Value
    end,
})

WindowESP:AddColorPicker("WindowESPColor", {
    Default = WindowESPColor,
    Title = "Window Color",
    Transparency = 0,
    Callback = function(Value)
        WindowESPColor = Value
    end
})

-- Performance Settings
local PerfGroup = TabsUI.Esp:AddRightGroupbox("World Effects", "sun")

PerfGroup:AddToggle("FullBright", {
    Text = "Full Bright",
    Tooltip = "Bikin map jadi terang biar lebih jelas",
    Default = false,
    Callback = function(Value)
        FullBright = Value
        if Value then
            ApplyFullBright()
            Library:Notify({ Title = "Full Bright", Description = "Full Bright aktif!", Time = 3 })
        else
            Lighting.Brightness = 1
            Lighting.ClockTime = 14
            Lighting.Ambient = Color3.fromRGB(128,128,128)
            Lighting.OutdoorAmbient = Color3.fromRGB(128,128,128)
            Library:Notify({ Title = "Full Bright", Description = "Full Bright dimatikan", Time = 3 })
        end
    end,
})

PerfGroup:AddSlider("TimeOfDay", {
    Text = "Time Of Day",
    Tooltip = "Atur waktu di game",
    Default = 14,
    Min = 0,
    Max = 24,
    Rounding = 0,
    Callback = function(Value)
        TimeOfDayValue = Value
        if FullBright then Lighting.ClockTime = Value end
    end,
})

PerfGroup:AddDivider()

PerfGroup:AddToggle("NoFog", {
    Text = "No Fog",
    Tooltip = "Hapus kabut biar map lebih jelas",
    Default = false,
    Callback = function(Value)
        NoFog = Value
        if Value then
            ApplyNoFog()
            Library:Notify({ Title = "No Fog", Description = "Kabut berhasil dihilangkan!", Time = 3 })
        else
            Lighting.FogStart = 0
            Lighting.FogEnd = 1000
            local atmosphere = Lighting:FindFirstChildOfClass("Atmosphere")
            if atmosphere then atmosphere.Density = 0.35 end
            Library:Notify({ Title = "No Fog", Description = "No Fog dimatikan", Time = 3 })
        end
    end,
})

PerfGroup:AddToggle("NoShadow", {
    Text = "No Shadow",
    Tooltip = "Matikan shadow",
    Default = false,
    Callback = function(Value)
        NoShadow = Value
        ApplyNoShadow()
        Library:Notify({ Title = "No Shadow", Description = Value and "Shadow berhasil dihapus!" or "Shadow dikembalikan normal", Time = 3 })
    end,
})

PerfGroup:AddDivider()

PerfGroup:AddToggle("LowGraphics", {
    Text = "Low Graphics",
    Tooltip = "Turunin grafik biar lebih ringan",
    Default = false,
    Callback = function(Value)
        LowGraphics = Value
        if Value then
            for _, obj in pairs(workspace:GetDescendants()) do ApplyLowGraphicsToObject(obj) end
            Library:Notify({ Title = "Low Graphics", Description = "Low Graphics aktif!", Time = 3 })
        else
            for obj, data in pairs(savedGraphics) do
                if obj and obj.Parent then
                    for prop, val in pairs(data) do obj[prop] = val end
                end
            end
            savedGraphics = {}
            Library:Notify({ Title = "Low Graphics", Description = "Low Graphics dimatikan", Time = 3 })
        end
    end,
})

PerfGroup:AddToggle("FPSBoost", {
    Text = "FPS Boost (Extreme)",
    Tooltip = "Boost FPS",
    Default = false,
    Callback = function(Value)
        FPSBoost = Value
        if Value then
            for _, obj in pairs(workspace:GetDescendants()) do ApplyFPSBoostToObject(obj) end
            Library:Notify({ Title = "FPS Boost", Description = "FPS Boost aktif!", Time = 3 })
        else
            for obj, state in pairs(savedEffects) do
                if obj and obj.Parent then obj.Enabled = state end
            end
            savedEffects = {}
            Library:Notify({ Title = "FPS Boost", Description = "FPS Boost dimatikan", Time = 3 })
        end
    end,
})


-- ==================== TAB TELEPORT ====================
local TeleportPlayerGroup = TabsUI.Teleport:AddLeftGroupbox("Teleport Players", "users")

local selectedPlayer = nil
local playerList = {}

local function UpdatePlayerList()
    playerList = {}
    for _, plr in pairs(Players:GetPlayers()) do 
        if plr ~= player then 
            table.insert(playerList, plr.Name) 
        end 
    end
end
UpdatePlayerList()

TeleportPlayerGroup:AddDropdown("PlayerDropdown", {
    Values = playerList,
    Default = "",
    Text = "Pilih Player",
    Tooltip = "TP ke player yang di pilih",
    Callback = function(Value)
        selectedPlayer = Value
    end,
})

TeleportPlayerGroup:AddButton({
    Text = "Refresh Player",
    Func = function()
        UpdatePlayerList()
        Library:Notify({ Title = "Teleport", Description = "List player direfresh!", Time = 3 })
    end,
})

TeleportPlayerGroup:AddButton({
    Text = "Teleport ke player yang dipilih",
    Func = function()
        if not selectedPlayer or selectedPlayer == "" then
            Library:Notify({ Title = "Error", Description = "Pilih pemain terlebih dulu!", Time = 3 })
            return
        end
        local target = Players:FindFirstChild(selectedPlayer)
        if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
            local char = player.Character
            if char and char:FindFirstChild("HumanoidRootPart") then
                char.HumanoidRootPart.CFrame = target.Character.HumanoidRootPart.CFrame + Vector3.new(0,3,0)
            end
        end
    end,
})

-- Teleport Maps
local TeleportMapGroup = TabsUI.Teleport:AddRightGroupbox("Teleport Maps", "map")

local genIndex, hookIndex, gateIndex, palletIndex, windowIndex = 1,1,1,1,1

local function TeleportToPart(part)
    local char = player.Character
    if char and char:FindFirstChild("HumanoidRootPart") then
        char.HumanoidRootPart.CFrame = part.CFrame + Vector3.new(0,5,0)
    end
end

TeleportMapGroup:AddButton({
    Text = "TP Generator (Loop)",
    Func = function()
        if #cache.Generator == 0 then
            for _, v in pairs(workspace:GetDescendants()) do AddToCache(v) end
        end
        if genIndex > #cache.Generator then genIndex = 1 end
        if cache.Generator[genIndex] and cache.Generator[genIndex].part then 
            TeleportToPart(cache.Generator[genIndex].part) 
        end
        genIndex = genIndex + 1
    end,
})

TeleportMapGroup:AddButton({
    Text = "TP Hook (Loop)",
    Func = function()
        if #cache.Hook == 0 then
            for _, v in pairs(workspace:GetDescendants()) do AddToCache(v) end
        end
        if hookIndex > #cache.Hook then hookIndex = 1 end
        if cache.Hook[hookIndex] and cache.Hook[hookIndex].part then 
            TeleportToPart(cache.Hook[hookIndex].part) 
        end
        hookIndex = hookIndex + 1
    end,
})

TeleportMapGroup:AddButton({
    Text = "TP Gate (Loop)",
    Func = function()
        if #cache.Gate == 0 then
            for _, v in pairs(workspace:GetDescendants()) do AddToCache(v) end
        end
        if gateIndex > #cache.Gate then gateIndex = 1 end
        if cache.Gate[gateIndex] and cache.Gate[gateIndex].part then 
            TeleportToPart(cache.Gate[gateIndex].part) 
        end
        gateIndex = gateIndex + 1
    end,
})

TeleportMapGroup:AddButton({
    Text = "TP Pallet (Loop)",
    Func = function()
        if #cache.Pallet == 0 then
            for _, v in pairs(workspace:GetDescendants()) do AddToCache(v) end
        end
        if palletIndex > #cache.Pallet then palletIndex = 1 end
        if cache.Pallet[palletIndex] and cache.Pallet[palletIndex].part then 
            TeleportToPart(cache.Pallet[palletIndex].part) 
        end
        palletIndex = palletIndex + 1
    end,
})

TeleportMapGroup:AddButton({
    Text = "TP Window (Loop)",
    Func = function()
        if #cache.Window == 0 then
            for _, v in pairs(workspace:GetDescendants()) do AddToCache(v) end
        end
        if windowIndex > #cache.Window then windowIndex = 1 end
        if cache.Window[windowIndex] and cache.Window[windowIndex].part then 
            TeleportToPart(cache.Window[windowIndex].part) 
        end
        windowIndex = windowIndex + 1
    end,
})

TeleportMapGroup:AddButton({
    Text = "Refresh Map",
    Func = function()
        cache.Generator = {} 
        cache.Hook = {} 
        cache.Gate = {} 
        cache.Window = {} 
        cache.Pallet = {} 
        cache.Zombie = {}
        for _, v in pairs(workspace:GetDescendants()) do AddToCache(v) end
        Library:Notify({ Title = "Map", Description = "Map Cache Refreshed", Time = 3 })
    end,
})

-- ==================== TAB MISC ====================
-- Player Emote
local EmoteGroup = TabsUI.Misc:AddLeftGroupbox("Player Emote", "smile")

EmoteGroup:AddToggle("EmoteEnabled", {
    Text = "Aktifkan Emote",
    Default = false,
    Callback = function(Value)
        EmoteEnabled = Value
        if Value then 
            PlayEmote() 
        else 
            StopEmote() 
        end
    end,
})

EmoteGroup:AddDropdown("EmoteSelect", {
    Values = {
        "Friday Night",
        "WarCry",
        "24 Hour Cinderella",
        "Applause",
        "Arm Swing",
        "Backflip",
        "California Girls",
        "Christmas Spirit",
        "Floating Rest",
        "Ghoul",
        "Griddy",
        "Kyoufuu",
        "OnePlays",
        "Vulnerable"
    },

    Default = "Friday Night",
    Text = "Pilih Emote",

    Callback = function(Value)

        if Value == "Friday Night" then
            SelectedAnim = "rbxassetid://83229063951016"
            SelectedSound = "rbxassetid://85355610204255"
            
         elseif Value == "WarCry" then
            SelectedAnim = "rbxassetid://82600868380136"
            SelectedSound = "rbxassetid://120101930689931"

        elseif Value == "24 Hour Cinderella" then 
            SelectedAnim = "rbxassetid://137195203725366" 
            SelectedSound = "rbxassetid://121099446613414"

        elseif Value == "Applause" then 
            SelectedAnim = "rbxassetid://96328361165090" 
            SelectedSound = "rbxassetid://115490787020749"

        elseif Value == "Arm Swing" then 
            SelectedAnim = "rbxassetid://80552139463944" 
            SelectedSound = "rbxassetid://74216458932348"

        elseif Value == "Backflip" then 
            SelectedAnim = "rbxassetid://74705617908505" 
            SelectedSound = nil

        elseif Value == "California Girls" then 
            SelectedAnim = "rbxassetid://123552803041504" 
            SelectedSound = "rbxassetid://87899327891544"

        elseif Value == "Christmas Spirit" then 
            SelectedAnim = "rbxassetid://137859761110514" 
            SelectedSound = nil

        elseif Value == "Floating Rest" then 
            SelectedAnim = "rbxassetid://114593021219597" 
            SelectedSound = nil

        elseif Value == "Ghoul" then 
            SelectedAnim = "rbxassetid://130415594909401" 
            SelectedSound = "rbxassetid://123004139176580"

        elseif Value == "Griddy" then 
            SelectedAnim = "rbxassetid://75586690784894" 
            SelectedSound = nil

        elseif Value == "Kyoufuu" then 
            SelectedAnim = "rbxassetid://137322894494527" 
            SelectedSound = "rbxassetid://129064643026442"

        elseif Value == "OnePlays" then
            SelectedAnim = "rbxassetid://140625405103474"
            SelectedSound = "rbxassetid://94749073728335"

        elseif Value == "Vulnerable" then
            SelectedAnim = "rbxassetid://121773684313913"
            SelectedSound = "rbxassetid://135265751184744"
        end

        if EmoteEnabled then
            PlayEmote()
        end
    end,
})

-- Player Misc
local MiscPlayerGroup = TabsUI.Misc:AddLeftGroupbox("Player Misc", "user")

MiscPlayerGroup:AddToggle("SpeedBoost", {
    Text = "Enable Speed Boost",
    Default = false,
    Callback = function(Value)
        SpeedEnabled = Value
    end,
})

MiscPlayerGroup:AddSlider("SpeedAmount", {
    Text = "Speed Boost Value",
    Default = 0.02,
    Min = 0.01,
    Max = 3,
    Rounding = 2,
    Callback = function(Value)
        SpeedAmount = Value
    end,
})

MiscPlayerGroup:AddDivider()

MiscPlayerGroup:AddInput("SpeedInputValue", {
    Text = "Speed Value (Input Mode)",
    Tooltip = "Masukkan angka kecepatan (contoh: 1.5, 2, 3)",
    Default = "0.1",
    Placeholder = "0.1",
    Numeric = true,
    Callback = function(Value)
        local num = tonumber(Value)
        if num then
            SpeedInputValue = num
            Library:Notify({ Title = "Speed", Description = "Kecepatan diubah ke " .. num, Time = 2 })
        end
    end,
})

MiscPlayerGroup:AddToggle("SpeedInputMode", {
    Text = "Enable Speed Boost (Input Mode)",
    Tooltip = "Aktifkan speed boost dengan kecepatan di atas",
    Default = false,
    Callback = function(Value)
        SpeedInputEnabled = Value
        if Value then
            startSpeedInputMode()
            Library:Notify({ Title = "Speed Input", Description = "Speed Boost aktif! (" .. SpeedInputValue .. ")", Time = 3 })
        else
            stopSpeedInputMode()
            Library:Notify({ Title = "Speed Input", Description = "Speed Boost dimatikan", Time = 2 })
        end
    end,
})

MiscPlayerGroup:AddDivider()

MiscPlayerGroup:AddButton({
    Text = "Open Global ChatGUI",
    Func = function()
        local success, err = pcall(function()
            loadstring(game:HttpGet("https://pastefy.app/nmSSS371/raw"))()
        end)
        if success then
            Library:Notify({ Title = "Global Chat", Description = "GUI Global Chat berhasil dimuat!", Time = 3 })
        else
            Library:Notify({ Title = "Error", Description = "Gagal memuat Global Chat!", Time = 3 })
        end
    end
})

MiscPlayerGroup:AddButton({
    Text = "Open Moonwalk GUI",
    Func = function()
        local success, err = pcall(function()
            loadstring(game:HttpGet("https://pastebin.com/raw/JWr0bW8u"))()
        end)
        if success then
            Library:Notify({ Title = "Moonwalk", Description = "GUI Moonwalk berhasil dimuat!", Time = 3 })
        else
            Library:Notify({ Title = "Error", Description = "Gagal memuat Moonwalk!", Time = 3 })
        end
    end
})

MiscPlayerGroup:AddButton({
    Text = "Fly GUI",
    Func = function()
        local found = false
        for _, v in pairs(game.CoreGui:GetChildren()) do
            if v.Name:lower():find("fly") then found = true end
        end
        if not found then
            loadstring(game:HttpGet("https://raw.githubusercontent.com/XNEOFF/FlyGuiV3/main/FlyGuiV3.txt"))()
            Library:Notify({ Title = "Fly", Description = "Fly GUI Dimuat", Time = 3 })
        else
            Library:Notify({ Title = "Fly", Description = "Fly GUI terbuka", Time = 3 })
        end
    end
})

MiscPlayerGroup:AddDivider()

MiscPlayerGroup:AddToggle("Noclip", {
    Text = "No Clip",
    Tooltip = "Tembus semua objek",
    Default = false,
    Callback = function(Value)
        NoclipEnabled = Value
        if Value then
            if NoclipConnection then NoclipConnection:Disconnect() end
            NoclipConnection = RunService.RenderStepped:Connect(function()
                local char = player.Character
                if char then
                    for _, part in ipairs(char:GetDescendants()) do
                        if part:IsA("BasePart") then
                            part.CanCollide = false
                        end
                    end
                end
            end)
            Library:Notify({ Title = "No Clip", Description = "No Clip Enabled", Time = 3 })
        else
            if NoclipConnection then NoclipConnection:Disconnect() end
            NoclipConnection = nil
            local char = player.Character
            if char then
                for _, part in ipairs(char:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.CanCollide = true
                    end
                end
            end
            Library:Notify({ Title = "No Clip", Description = "No Clip Disabled", Time = 3 })
        end
    end,
})

MiscPlayerGroup:AddToggle("AntiAFK", {
    Text = "Anti AFK",
    Tooltip = "Biar nggak kena kick pas AFK",
    Default = true,
    Callback = function(Value)
        if Value then
            task.spawn(function()
                while Value do
                    pcall(function()
                        game:GetService("VirtualUser"):Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
                        task.wait(1)
                        game:GetService("VirtualUser"):Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
                    end)
                    task.wait(60)
                end
            end)
        end
    end,
})

MiscPlayerGroup:AddDivider()

MiscPlayerGroup:AddButton({
    Text = "Rejoin Server",
    Func = function()
        TeleportService:Teleport(game.PlaceId, player)
    end,
})

local CrossGroup = TabsUI.Misc:AddRightGroupbox("Crosshair", "crosshair")

local CrosshairToggle = CrossGroup:AddToggle("Crosshair", {
    Text = "Enable Crosshair",
    Default = false,
    Callback = function(Value)
        CrosshairEnabled = Value
    end,
})

CrosshairToggle:AddColorPicker("CrosshairColor", {
    Default = CrosshairColor,
    Title = "Crosshair Color",
    Transparency = 0,
    Callback = function(Value)
        CrosshairColor = Value
    end
})

CrossGroup:AddDropdown("CrosshairStyle", {
    Values = { "Dot", "Plus", "X", "Box" },
    Default = "Dot",
    Multi = false,
    Text = "Crosshair Style",
    Callback = function(Value)
        CrosshairStyle = Value
    end,
})

CrossGroup:AddSlider("CrosshairSize", {
    Text = "Crosshair Size",
    Default = 3,
    Min = 1,
    Max = 100,
    Rounding = 0,
    Callback = function(Value)
        CrosshairSize = Value
    end,
})

CrossGroup:AddSlider("CrosshairThickness", {
    Text = "Crosshair Thickness",
    Default = 4,
    Min = 1,
    Max = 20,
    Rounding = 0,
    Callback = function(Value)
        CrosshairThickness = Value
    end,
})

CrossGroup:AddSlider("CrosshairGap", {
    Text = "Crosshair Gap",
    Default = 6,
    Min = 0,
    Max = 50,
    Rounding = 0,
    Callback = function(Value)
        CrosshairGap = Value
    end,
})

CrossGroup:AddSlider("Crosshair Position X", {
    Text = "Position X",
    Default = 0,
    Min = -500,
    Max = 500,
    Rounding = 0,
    Callback = function(Value)
        CrosshairPosX = Value
    end,
})

CrossGroup:AddSlider("Crosshair Position Y", {
    Text = "Position Y",
    Default = 0,
    Min = -500,
    Max = 500,
    Rounding = 0,
    Callback = function(Value)
        CrosshairPosY = Value
    end,
})

local AttributeEditorEnabled = false

local FunGroup = TabsUI.Misc:AddRightGroupbox("Fun", "sparkles")

FunGroup:AddToggle("AttributeEditor", {
    Text = "Edit Attributes",
    Default = false,

    Callback = function(Value)
        AttributeEditorEnabled = Value

        Library:Notify({
            Title = "Attribute Editor",
            Description = Value and "✅ Enabled" or "❌ Disabled",
            Time = 3
        })
    end
})

FunGroup:AddInput("SetLevel", {
    Text = "Level",
    Placeholder = "Enter Level",
    Numeric = true,

    Callback = function(Value)
        if not AttributeEditorEnabled then
            Library:Notify({
                Title = "Attribute Editor",
                Description = "Enable Edit Attributes first!",
                Time = 3
            })
            return
        end

        local Amount = tonumber(Value)

        if Amount then
            player:SetAttribute("Level", Amount)

            Library:Notify({
                Title = "Level Updated",
                Description = "Level set to " .. Amount,
                Time = 3
            })
        end
    end
})

FunGroup:AddInput("SetGears", {
    Text = "Gears",
    Placeholder = "Enter Gears",
    Numeric = true,

    Callback = function(Value)
        if not AttributeEditorEnabled then
            Library:Notify({
                Title = "Attribute Editor",
                Description = "Enable Edit Attributes first!",
                Time = 3
            })
            return
        end

        local Amount = tonumber(Value)

        if Amount then
            player:SetAttribute("Gears", Amount)

            Library:Notify({
                Title = "Gears Updated",
                Description = "Gears set to " .. Amount,
                Time = 3
            })
        end
    end
})

FunGroup:AddInput("SetScrews", {
    Text = "Screws",
    Placeholder = "Enter Screws",
    Numeric = true,

    Callback = function(Value)
        if not AttributeEditorEnabled then
            Library:Notify({
                Title = "Attribute Editor",
                Description = "Enable Edit Attributes first!",
                Time = 3
            })
            return
        end

        local Amount = tonumber(Value)

        if Amount then
            player:SetAttribute("Screws", Amount)

            Library:Notify({
                Title = "Screws Updated",
                Description = "Screws set to " .. Amount,
                Time = 3
            })
        end
    end
})
local  SpoofGroup = TabsUI.Misc:AddRightGroupbox("Name Spoofer", "circle-user-round")
    SpoofGroup:AddToggle("Misc_FakeName", { Text = "Fake Name", Default = false, Callback = function(Value) Config.Misc_FakeName = Value; if Value then enableSpoofer() else disableSpoofer() end end })
    
local MenuCon = TabsUI.Misc:AddRightGroupbox("Menu keybind", "user-plus")
menuCon:AddLabel("Menu bind"):AddKeyPicker("MenuKeybind", { Default = "RightShift", NoUI = true, Text = "Menu keybind" })
    Library.ToggleKeybind = Options.MenuKeybind 
    Library.ShowToggleFrameInKeybinds = true 
    
-- Copy Avatar
local CopyAvatarGroup = TabsUI.Misc:AddRightGroupbox("Copy Avatar", "user-plus")

CopyAvatarGroup:AddButton({
    Text = "BUKA COPY AVATAR",
    Func = function()
        pcall(function()
            loadstring(game:HttpGet("https://pastefy.app/BcXlgyHh/raw"))()
        end)
        Library:Notify({ Title = "Copy Avatar", Description = "GUI Copy Avatar dibuka!", Time = 3 })
    end,
})

CopyAvatarGroup:AddLabel("Cara Pakai:\n1. Klik tombol di atas\n2. GUI Copy Avatar akan muncul\n3. Masukkan username/ID player\n4. Klik APPLY AVATAR\n5. Tutup GUI dengan klik X di pojok kanan", true)

-- ==================== INITIALIZATION ====================
-- Setup Auto Reconnect

local function SetupAutoReconnect()
    TeleportService.TeleportInitFailed:Connect(function(player, teleportResult, errorMessage)
        if Config.AUTO_AFKEscape then
            if getgenv().Library then 
                getgenv().Library:Notify("Teleport Gagal: " .. tostring(errorMessage) .. ". Mencoba server lain...", 3) 
            end
            task.wait(2)
            RandomServerHop()
        end
    end)

    pcall(function()
        local promptOverlay = CoreGui:WaitForChild("RobloxPromptGui", 5):WaitForChild("promptOverlay", 5)
        if promptOverlay then
            promptOverlay.ChildAdded:Connect(function(child)
                if Config.AUTO_AFKEscape and child.Name == "ErrorPrompt" then
                    if getgenv().Library then 
                        getgenv().Library:Notify("Server Penuh/Disconnect! Mencoba server hop...", 3) 
                    end
                    
                    pcall(function() child.Visible = false end)
                    
                    task.wait(2.5)
                    RandomServerHop()
                end
            end)
        end
    end)
end

SetupAutoReconnect()

LocalPlayer:GetPropertyChangedSignal("Team"):Connect(function()
    HandleAFKLogic(LocalPlayer.Team and LocalPlayer.Team.Name or "")
    if LocalPlayer.Team and LocalPlayer.Team.Name == "Survivors" then
        InitializeAutoSkillCheck()
    end
end)

-- Initialize Auto Skill Check
InitializeAutoSkillCheck()

LocalPlayer.CharacterAdded:Connect(function()
    if HeartbeatConnection then HeartbeatConnection:Disconnect() end
    if VisibilityConnection then VisibilityConnection:Disconnect() end
    task.wait(1.2)
    InitializeAutoSkillCheck()
    ReapplyPerformance()
    if Config.Surv_InstanSkillCheck then
        task.spawn(function()
            local genScript = char:WaitForChild("Skillcheck-gen", 3)
            local playerScript = char:WaitForChild("Skillcheck-player", 3)
            if genScript then genScript.Disabled = true end
            if playerScript then playerScript.Disabled = true end
        end)
    end
end)

-- Setup Parry Sensor
for _, p in pairs(Players:GetPlayers()) do 
    SetupPlayer(p) 
end
Players.PlayerAdded:Connect(SetupPlayer)

task.spawn(function()
    while true do 
        task.wait(5) 
        for _, p in pairs(Players:GetPlayers()) do 
            TryAttach(p) 
        end 
    end
end)

-- Cursor Mode
local CursorEnabled = false
local CursorConnection

UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == Enum.KeyCode.RightAlt then
        CursorEnabled = not CursorEnabled
        if CursorEnabled then
            UserInputService.MouseIconEnabled = true
            UserInputService.MouseBehavior = Enum.MouseBehavior.Default
            if CursorConnection then CursorConnection:Disconnect() end
            CursorConnection = RunService.RenderStepped:Connect(function()
                if UserInputService.MouseBehavior ~= Enum.MouseBehavior.Default then
                    UserInputService.MouseBehavior = Enum.MouseBehavior.Default
                end
                if not UserInputService.MouseIconEnabled then
                    UserInputService.MouseIconEnabled = true
                end
            end)
        else
            if CursorConnection then CursorConnection:Disconnect() end
            CursorConnection = nil
            UserInputService.MouseIconEnabled = false
            UserInputService.MouseBehavior = Enum.MouseBehavior.LockCenter
        end
    end
end)

-- Auto Parry Keybind (P)
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.P then
        Config.Surv_AutoParry = not Config.Surv_AutoParry
        Library:Notify({ 
            Title = "Auto Parry", 
            Description = Config.Surv_AutoParry and "AUTO PARRY AKTIF" or "Auto Parry MATI",
            Time = 2.5
        })
    end
end)

-- ==================== MAIN LOOP ====================
RunService.RenderStepped:Connect(function()
    local char = player.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")
    FleeKiller()
    
    -- Silent Aim Lock
    if isChargingPistol then
        if lockedTarget and AimConfig.LockAim and lockedTarget.Parent and lockedTarget.Parent:FindFirstChild("Humanoid") and lockedTarget.Parent.Humanoid.Health > 0 then
            faceTarget(lockedTarget) 
        else
            lockedTarget = getPistolTarget()
        end
    end
    
    -- Skill Check Attributes
    if player.Team and player.Team.Name == "Survivors" then
        if char:GetAttribute("skillcheckfrequency") ~= ConfigData.Surv_SkillFrequency then
            char:SetAttribute("skillcheckfrequency", ConfigData.Surv_SkillFrequency)
        end
        if char:GetAttribute("skillcheckspeed") ~= ConfigData.Surv_SkillSpeed then
            char:SetAttribute("skillcheckspeed", ConfigData.Surv_SkillSpeed)
        end
    end
    
    -- God Mode
    if GodMode and hum then
        pcall(function() 
            if hum.Health < hum.MaxHealth then
                hum.Health = hum.MaxHealth 
            end
        end)
    end
    
    -- Auto Wiggle
    if AutoWiggleEnabled then
        local carried = (char:FindFirstChild("IsCarried") and char.IsCarried.Value) or 
                       (char:FindFirstChild("IsCarrying") and char.IsCarrying.Value)
        if carried then
            local remotes = ReplicatedStorage:FindFirstChild("Remotes")
            if remotes then
                local carry = remotes:FindFirstChild("Carry")
                if carry then
                    local event = carry:FindFirstChild("SelfUnHookEvent")
                    if event then
                        for i = 1, 5 do event:FireServer() end
                    end
                end
            end
        end
    end
    
    -- Auto Carry & Hook
    if AutoCarryEnabled and not busy then
        busy = true
        local target = GetDownedChar()
        if target then
            local tRoot = target:FindFirstChild("HumanoidRootPart")
            if tRoot and hrp then
                hrp.CFrame = tRoot.CFrame * CFrame.new(0, 3, -2)
                task.wait(0.45)
                if CarryEvent then
                    for i = 1, 4 do 
                        CarryEvent:FireServer(target) 
                        task.wait(0.22) 
                    end
                end
                task.wait(0.75)
                local hook = GetNearestHook()
                if hook and hrp then
                    hrp.CFrame = hook.CFrame * CFrame.new(0, 4, -3)
                    task.wait(0.85)
                    if HookEvent then
                        for i = 1, 6 do 
                            HookEvent:FireServer(hook) 
                            task.wait(0.17) 
                        end
                    end
                end
            end
        end
        task.wait(2.5)
        busy = false
    end
    
    -- Auto Attack
    if AutoAttackEnabled then
        local target, dist = GetClosestEnemy()
        if target and dist and dist <= AttackRange then
            pcall(function() 
                local remotes = ReplicatedStorage:FindFirstChild("Remotes")
                if remotes then
                    local attacks = remotes:FindFirstChild("Attacks")
                    if attacks and attacks:FindFirstChild("BasicAttack") then
                        attacks.BasicAttack:FireServer(false) 
                    end
                end
            end)
        end
    end
    
    -- Auto Kill All
    if AutoKillAllEnabled and (player.Team and player.Team.Name:find("Killer")) then
        local myHRP = hrp
        if not myHRP then return end
        if not currentTarget or not currentTarget:FindFirstChild("Humanoid") or currentTarget.Humanoid.Health <= 35 then
            currentTarget = GetNearestAliveSurvivor()
        end
        if currentTarget then
            local targetHRP = currentTarget:FindFirstChild("HumanoidRootPart")
            if targetHRP then
                local dist = (targetHRP.Position - myHRP.Position).Magnitude
                if dist <= KillRange then
                    local velocity = targetHRP.AssemblyLinearVelocity
                    local predict = velocity * 0.15
                    local targetPos = targetHRP.Position + predict
                    local behind = targetHRP.CFrame.LookVector * -3
                    myHRP.CFrame = CFrame.new(targetPos + behind, targetPos)
                end
            end
            pcall(function() 
                local remotes = ReplicatedStorage:FindFirstChild("Remotes")
                if remotes then
                    local attacks = remotes:FindFirstChild("Attacks")
                    if attacks and attacks:FindFirstChild("BasicAttack") then
                        attacks.BasicAttack:FireServer(false) 
                    end
                end
            end)
            task.wait(AttackDelay)
        end
    else
        currentTarget = nil
    end
    
    -- ESP System
    local now = tick()
    if now - lastESPScan >= 2.5 then
        lastESPScan = now
        
        if ShouldShowESP() then
            -- Player ESP
            for _, plr in pairs(Players:GetPlayers()) do
    if plr ~= player and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
        local plrHRP = plr.Character.HumanoidRootPart
        local team = plr.Team and plr.Team.Name:lower() or ""

        if team:find("survivor") then
            ApplyHighlight(plr.Character, SurvivorESPColor, espSurvivor)
            CreateESP(plrHRP, plr.Name, SurvivorESPColor, espSurvivor, espName, espDistance)

        elseif team:find("killer") then
            local killerName = plr:GetAttribute("SelectedKiller") or (plr.Character and plr.Character:GetAttribute("SelectedKiller"))
            local displayName = (killerName and killerName ~= "") and killerName or plr.Name

            ApplyHighlight(plr.Character, KillerESPColor, espKiller)
            CreateESP(plrHRP, displayName, KillerESPColor, espKiller, espName, espDistance)
        end
    end
end

-- Killer Warning
if hrp then
    local warn = hrp:FindFirstChild("KillerWarn")

    if not killerWarnEnabled then
        if warn then warn.Enabled = false end
    else
        local killerNearby = false

        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                local team = p.Team and p.Team.Name:lower() or ""

                if team:find("killer") then
                    local dist = (p.Character.HumanoidRootPart.Position - hrp.Position).Magnitude

                    if dist < 99 then
                        killerNearby = true
                        break
                    end
                end
            end
        end

        if killerNearby then
            if not warn then
                warn = Instance.new("BillboardGui")
                warn.Name = "KillerWarn"
                warn.Size = UDim2.new(0, 50, 0, 50)
                warn.AlwaysOnTop = true
                warn.StudsOffset = Vector3.new(0, 4, 0)
                warn.Parent = hrp

                local txt = Instance.new("TextLabel")
                txt.Size = UDim2.new(1,0,1,0)
                txt.BackgroundTransparency = 1
                txt.Text = "!!"
                txt.TextScaled = true
                txt.TextColor3 = Color3.fromRGB(255, 0, 0)
                txt.TextStrokeTransparency = 0
                txt.Font = Enum.Font.GothamBold
                txt.Parent = warn
            end

            warn.Enabled = true
        else
            if warn then warn.Enabled = false end
        end
    end
end

-- Generator ESP
for _, data in pairs(cache.Generator) do
    if data and data.obj and data.part then
        if espGen then
            ApplyHighlight(data.obj, GeneratorESPColor, true)

            local progress = 0
            local genObj = data.obj

            local attr = genObj:GetAttribute("RepairProgress")
            if not attr then
                attr = genObj:GetAttribute("Progress")
            end

            if attr then
                progress = attr
            else
                local bar = genObj:FindFirstChild("Bar")

                if bar and bar:IsA("BasePart") then
                    progress = (bar.Size.X / 10) * 100
                end
            end

            progress = math.clamp(progress, 0, 100)

            local part = data.part
            local bill = part:FindFirstChild("ESP_TEXT")

            if not bill then
                bill = Instance.new("BillboardGui")
                bill.Name = "ESP_TEXT"
                bill.Size = UDim2.new(0, 140, 0, 30)
                bill.AlwaysOnTop = true
                bill.StudsOffset = Vector3.new(0, 2.5, 0)
                bill.Parent = part

                local txt = Instance.new("TextLabel")
                txt.Name = "TextLabel"
                txt.Size = UDim2.new(1, 0, 1, 0)
                txt.BackgroundTransparency = 1
                txt.TextStrokeTransparency = 0
                txt.Font = Enum.Font.GothamBold
                txt.TextSize = 11
                txt.Parent = bill
            end

            bill.Enabled = true

            local txt = bill:FindFirstChild("TextLabel")

            if txt then
                txt.TextColor3 = GeneratorESPColor

                if progress >= 99.5 then
                    txt.Text = "✅ GEN DONE"
                else
                    txt.Text = "GEN " .. math.floor(progress) .. "%"
                end
            end
        else
            ApplyHighlight(data.obj, GeneratorESPColor, false)

            local bill = data.part:FindFirstChild("ESP_TEXT")
            if bill then
                bill.Enabled = false
            end
        end
    end
end

-- Hook ESP
for _, data in pairs(cache.Hook) do
    if data and data.obj and data.part then
        ApplyHighlight(data.obj, HookESPColor, espHook)
        CreateESP(data.part, "HOOK", HookESPColor, espHook, false, false)
    end
end

-- Gate ESP
for _, data in pairs(cache.Gate) do
    if data and data.obj and data.part then
        ApplyHighlight(data.obj, GateESPColor, espGate)
        CreateESP(data.part, "GATE", GateESPColor, espGate, false, false)
    end
end

-- Pallet ESP
for _, data in pairs(cache.Pallet) do
    if data and data.obj and data.part then
        ApplyHighlight(data.obj, PalletESPColor, espPallet)
        CreateESP(data.part, "PALLET", PalletESPColor, espPallet, false, false)
    end
end

-- Window ESP
for _, data in pairs(cache.Window) do
    if data and data.obj and data.part then
        ApplyHighlight(data.obj, WindowESPColor, espWindow)
        CreateESP(data.part, "WINDOW", WindowESPColor, espWindow, false, false)
    end
end

-- Zombie ESP
for _, data in pairs(cache.Zombie) do
    if data and data.obj and data.part then
        if espZombie then
            ApplyHighlight(data.obj, ZombieESPColor, true)
            CreateESP(data.part, "🧟 SCP Zombie", ZombieESPColor, true, true, false)
        else
            ApplyHighlight(data.obj, ZombieESPColor, false)

            local bill = data.part:FindFirstChild("ESP_TEXT")
            if bill then
                bill.Enabled = false
            end
        end
    end
end
        else
            HideAllESP()
        end
    end
    
    -- FOV
    if FOVEnabled and Camera then
        Camera.FieldOfView = TargetFOV
    end
    
    -- Speed Boost Normal
    if SpeedEnabled and hum and hrp then
        local moveDir = hum.MoveDirection
        if moveDir.Magnitude > 0 then            hrp.CFrame = hrp.CFrame + (moveDir * SpeedAmount)
        end
    end
    
    -- Silent Aim FOV Circles
    local cam = workspace.CurrentCamera
    local myChar = player.Character
    local isSpearMode = myChar and myChar:GetAttribute("spearmode") == true
    
    if AimConfig.Aim_SilentVeil and AimConfig.Veil_ShowFOV and isSpearMode then
        VeilFOVCircle.Visible = true
        VeilFOVCircle.Radius = AimConfig.Veil_FOV
        VeilFOVCircle.Position = Vector2.new(cam.ViewportSize.X / 2, cam.ViewportSize.Y / 2)
    else
        VeilFOVCircle.Visible = false
    end

    if AimConfig.Aim_Silent and AimConfig.Pistol_ShowFOV and AimConfig.Pistol_FOVMode then
        PistolFOVCircle.Visible = true
        PistolFOVCircle.Radius = AimConfig.Pistol_FOV
        PistolFOVCircle.Position = Vector2.new(cam.ViewportSize.X / 2, cam.ViewportSize.Y / 2)
    else
        PistolFOVCircle.Visible = false
    end

-- Pistol Laser
if isChargingPistol and AimConfig.Aim_Silent then
    local targetPart = getPistolTarget()

    if targetPart and myChar and hum and not IsDowned(myChar) then
        local leftArm = myChar:FindFirstChild("Left Arm") or myChar:FindFirstChild("LeftHand")
        local startPos = leftArm and leftArm.Position or myChar:GetPivot().Position

        local targetPos = targetPart.Position
        local targetVel = targetPart.AssemblyLinearVelocity
        targetVel = Vector3.new(targetVel.X, 0, targetVel.Z)

        local distance = (targetPos - startPos).Magnitude
        local bulletSpeed = 400
        local timeToHit = distance / bulletSpeed

        local predictedPos = targetPos + (targetVel * timeToHit)
        local offset = Vector3.new(0, -1.2, 0)
        local endPos = predictedPos + offset

        pistolLaser.Parent = workspace
        pistolLaser.Transparency = HideSilentLaser and 1 or 0

        local newDist = (endPos - startPos).Magnitude

        if newDist > 0 then
            pistolLaser.Size = Vector3.new(0.05, 0.05, newDist)
            pistolLaser.CFrame = CFrame.new(startPos, endPos) * CFrame.new(0, 0, -newDist / 2)
        end
    else
        if pistolLaser.Parent ~= nil then
            pistolLaser.Parent = nil
        end
    end
else
    if pistolLaser.Parent ~= nil then
        pistolLaser.Parent = nil
    end
end

    -- Veil Target Highlight
    if isChargingSpear and AimConfig.Aim_SilentVeil and isSpearMode then
        local targetPart = getClosestSurvivor()
        if targetPart and targetPart.Parent then
            veilTargetHighlight.Parent = targetPart.Parent
        else
            veilTargetHighlight.Parent = nil
        end
    else
        veilTargetHighlight.Parent = nil
    end
    
    -- Auto Parry Circle ESP
    if Config.Surv_ParryCircle and Config.Surv_AutoParry and hrp then 
        if not State.AutoParryAdornment or State.AutoParryAdornment.Parent ~= hrp then 
            if State.AutoParryAdornment then State.AutoParryAdornment:Destroy() end
            State.AutoParryAdornment = Instance.new("CylinderHandleAdornment")
            State.AutoParryAdornment.Name = "AutoParryCircleESP"
            State.AutoParryAdornment.Height = 0.05
            State.AutoParryAdornment.Transparency = 0.3
            State.AutoParryAdornment.Adornee = hrp
            State.AutoParryAdornment.Parent = hrp
            State.AutoParryAdornment.ZIndex = 0
            State.AutoParryAdornment.AlwaysOnTop = false 
        end
        
        local cR = Config.Surv_ParryRadius
        State.AutoParryAdornment.Radius = cR
        State.AutoParryAdornment.InnerRadius = math.max(0.1, cR - 0.15)
        State.AutoParryAdornment.CFrame = CFrame.new(0, -3, 0) * CFrame.Angles(math.rad(90), 0, 0)
        
        if State.ParryCooldown then 
            State.AutoParryAdornment.Color3 = Color3.fromRGB(255, 128, 0) 
        elseif Config.Surv_ParryAggressive then 
            State.AutoParryAdornment.Color3 = Color3.fromRGB(255, 0, 0) 
        else 
            State.AutoParryAdornment.Color3 = Color3.fromRGB(0, 255, 255) 
        end 
    elseif State.AutoParryAdornment then 
        State.AutoParryAdornment:Destroy()
        State.AutoParryAdornment = nil 
    end
end)

-- ==================== SILENT AIM INPUT HANDLER ====================
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    local isTouch = (input.UserInputType == Enum.UserInputType.Touch)
    
    if gameProcessed and not isTouch then return end 
    
    local char = player.Character
    if IsDowned(char) then return end
    local isSpearMode = char and char:GetAttribute("spearmode") == true

    if input.UserInputType == Enum.UserInputType.MouseButton2 then
        if AimConfig.Aim_Silent then 
            isChargingPistol = true 
            lockedTarget = getPistolTarget()
        end
    elseif input.UserInputType == Enum.UserInputType.MouseButton1 then
        if AimConfig.Aim_Silent and isChargingPistol then 
            executeSilentAimFire() 
        end
        if AimConfig.Aim_SilentVeil and isSpearMode then 
            isChargingSpear = true 
        end
    elseif isTouch then
        if AimConfig.Aim_Silent then
            local playerGui = player:FindFirstChild("PlayerGui")
            if playerGui then
                local survivorMob = playerGui:FindFirstChild("Survivor-mob")
                if survivorMob then
                    local controls = survivorMob:FindFirstChild("Controls")
                    if controls then
                        local targetBtn = controls:FindFirstChild("Gui-mob")
                        if targetBtn and targetBtn.Visible then
                            local pos = input.Position
                            local absPos = targetBtn.AbsolutePosition
                            local absSize = targetBtn.AbsoluteSize
                            if pos.X >= absPos.X and pos.X <= (absPos.X + absSize.X) and pos.Y >= absPos.Y and pos.Y <= (absPos.Y + absSize.Y) then
                                isChargingPistol = true
                                currentTouchInput = input
                                lockedTarget = getPistolTarget()
                            end
                        end
                    end
                end
            end
        end
        
        if AimConfig.Aim_SilentVeil and isSpearMode then
            local playerGui = player:FindFirstChild("PlayerGui")
            if playerGui then
                local slasherMob = playerGui:FindFirstChild("Slasher-mob")
                if slasherMob then
                    local controls = slasherMob:FindFirstChild("Controls")
                    if controls then
                        local attackBtn = controls:FindFirstChild("attack")
                        if attackBtn and attackBtn.Visible then
                            local pos = input.Position
                            local absPos = attackBtn.AbsolutePosition
                            local absSize = attackBtn.AbsoluteSize
                            if pos.X >= absPos.X and pos.X <= (absPos.X + absSize.X) and pos.Y >= absPos.Y and pos.Y <= (absPos.Y + absSize.Y) then
                                isChargingSpear = true
                                currentTouchInput = input
                            end
                        end
                    end
                end
            end
        end
    end
end)

UserInputService.InputEnded:Connect(function(input, gameProcessed)
    local isTouchEnd = (input.UserInputType == Enum.UserInputType.Touch)
    
    if input.UserInputType == Enum.UserInputType.MouseButton2 then
        if isChargingPistol then 
            isChargingPistol = false 
            lockedTarget = nil
        end
    end

    if isChargingSpear and (input == currentTouchInput or input.UserInputType == Enum.UserInputType.MouseButton1) then
        isChargingSpear = false
        if currentTouchInput == input then currentTouchInput = nil end
        
        if isAttackCooldown then return end
        isAttackCooldown = true
        task.delay(2, function() isAttackCooldown = false end)
        
        local myChar = player.Character
        local startPart = myChar and (myChar:FindFirstChild("Head") or myChar:FindFirstChild("HumanoidRootPart"))
        
        if startPart then
            local startPos = startPart.Position
            local targetPart = getClosestSurvivor()
            local aimDirection
            
            local currentSpearSpeed = AimConfig.SPEAR_Speed
            
            if targetPart then
                local targetPos = targetPart.Position
                local targetVel = Vector3.new(0, 0, 0)
                if targetPart:IsA("BasePart") then
                    targetVel = targetPart.AssemblyLinearVelocity
                    targetVel = Vector3.new(targetVel.X, 0, targetVel.Z) 
                end
                local distance = (targetPos - startPos).Magnitude
                local timeToHit = distance / currentSpearSpeed 
                local dynamicPrediction = math.clamp(distance / 50, 0.1, 4.0)
                local predictedPos = targetPos + (targetVel * (timeToHit * dynamicPrediction))
                local distanceMultiplier = math.clamp(distance / 100, 1, 2.5)
                local autoGravity = math.max(0, distance - 8)
                local predictedPos = targetPos + (targetVel * timeToHit)
                local gravity = AimConfig.AIM_Auto and autoGravity or AimConfig.SPEAR_Gravity
                local dropCompensation = 0.5 * gravity * (timeToHit ^ 2) * distanceMultiplier
                local finalAimPos = predictedPos + Vector3.new(0, dropCompensation, 0)
                aimDirection = (finalAimPos - startPos).Unit
            else
                aimDirection = Camera.CFrame.LookVector
            end
            
            pcall(function()
                local args = { aimDirection, currentSpearSpeed, startPos }
                local remotes = ReplicatedStorage:FindFirstChild("Remotes")
                if remotes then
                    local killers = remotes:FindFirstChild("Killers")
                    if killers then
                        local veil = killers:FindFirstChild("Veil")
                        if veil and veil:FindFirstChild("Spearthrow") then
                            veil.Spearthrow:FireServer(unpack(args))
                        end
                    end
                end
            end)
            
            if VeilFOVCircle then VeilFOVCircle.Color = Color3.fromRGB(150, 150, 150) end
            
            if not isPassiveCooldown then
                isPassiveCooldown = true
                Library:Notify({ Title = "Pasif CD", Description = "Spear cooldown aktif", Time = 5 })
                task.delay(30, function()
                    if VeilFOVCircle then VeilFOVCircle.Color = Color3.fromRGB(0, 255, 100) end
                    isPassiveCooldown = false
                end)
            end
        end
    end
    
    if isTouchEnd and input == currentTouchInput then
        if isChargingPistol then
            isChargingPistol = false
            currentTouchInput = nil
            executeSilentAimFire()
            lockedTarget = nil
        end
    end
end)

-- ==================== UI SETTINGS & SAVE MANAGER ====================
ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)

SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ "MenuKeybind" })

ThemeManager:SetFolder("PanduHub")
SaveManager:SetFolder("PanduHub/SurviveTheKiller")

SaveManager:BuildConfigSection(TabsUI["UI Settings"])
ThemeManager:ApplyToTab(TabsUI["UI Settings"])

SaveManager:LoadAutoloadConfig()

-- ==================== NOTIFICATION ====================
Library:Notify({ Title = "Pandu Hub", Description = "Berhasil Dimuat! (Obsidian UI)", Time = 3 })